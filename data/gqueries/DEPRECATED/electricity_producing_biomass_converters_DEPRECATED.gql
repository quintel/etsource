# Returns the converters which produce electricity using biomoass. It works by looking to converters directly fed by the "biomass_distribution" converter that have output of electricity.

- deprecated_key = electricity_producing_converters_biomass
- unit = converters

FILTER(PARENTS(V(energy_distribution_bio_residues_for_firing )), "electricity_output_conversion && electricity_output_conversion > 0")
