# 

- deprecated_key = total_cost_of_primary_bio_ethanol
- unit = euro

PRODUCT(
  SUM(
    V(G(final_demand_cbs), primary_demand_of_bio_ethanol)
  )
  ,
  V(CARRIER(bio_ethanol),"cost_per_mj * sustainable")
)