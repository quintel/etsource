- deprecated_key = max_production_in_mw_wind_coastal
# 

- unit = mw

PRODUCT(
  DIVIDE(AREA(onshore_suitable_for_wind),V(energy_power_wind_turbine_coastal,land_use_per_unit)),
  V(energy_power_wind_turbine_coastal,"typical_electricity_production_capacity * full_load_hours / 8760")
)
