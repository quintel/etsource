# this query returns the present number of new residences calculated from the area data 'number_households' and
# 'percentage_of_new_houses'
# this query is used to set the max cap of the slider 'households_number_of_old_houses'

- query = AREA(number_households) * AREA(percentage_of_new_houses) / 100
- unit = number
