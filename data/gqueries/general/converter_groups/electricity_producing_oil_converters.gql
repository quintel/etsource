# Returns the converters which produce electricity using oil. It works by looking to converters directly fed by the "energy_distribution_crude_oil" converter that have output of electricity.

- query = FILTER(G(electricity_production), "crude_oil_input_conversion > 0")
- unit = converters
- deprecated_key = electricity_producing_converters_oil
