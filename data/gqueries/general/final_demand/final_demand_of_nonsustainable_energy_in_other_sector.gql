# sum of non_sustainable

- query =
    DIVIDE(
      SUM(
        V(INTERSECTION(SECTOR(other),G(final_demand_cbs)),input_of_biodiesel),
        V(INTERSECTION(SECTOR(other),G(final_demand_cbs)),input_of_bio_ethanol)
      ),
      BILLIONS
    )
- unit = pj
