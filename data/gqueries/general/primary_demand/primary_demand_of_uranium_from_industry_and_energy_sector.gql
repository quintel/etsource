- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(industry),G(final_demand_cbs)),INTERSECTION(SECTOR(energy),G(final_demand_cbs)),primary_demand_of_uranium_oxide).sum,
      BILLIONS
    )
- unit = pj
- deprecated_key = industry_uranium_primary_demand
