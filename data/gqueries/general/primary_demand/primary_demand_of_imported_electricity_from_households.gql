- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(households),G(final_demand_cbs)),primary_demand_of_imported_electricity).sum,
      BILLIONS
    )
- unit = pj
- deprecated_key = households_imported_e_primary_demand
