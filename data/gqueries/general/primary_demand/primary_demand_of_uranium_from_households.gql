- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(households),G(final_demand_cbs)),primary_demand_of_uranium_oxide).sum,
      BILLIONS
    )
- unit = pj
- deprecated_key = households_uranium_primary_demand
