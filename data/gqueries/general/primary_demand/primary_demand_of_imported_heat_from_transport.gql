- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(transport),G(final_demand_cbs)),primary_demand_of_imported_steam_hot_water).sum,
      BILLIONS
    )
- unit = pj
- deprecated_key = transport_imported_heat_primary_demand
