- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(agriculture),G(final_demand_cbs)),primary_demand_of_uranium_oxide).sum,
      BILLIONS
    )
- unit = pj
- deprecated_key = agriculture_uranium_primary_demand
