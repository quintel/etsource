# investment costs of energy_power_ultra_supercritical_lignite IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(energy_power_ultra_supercritical_lignite,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
