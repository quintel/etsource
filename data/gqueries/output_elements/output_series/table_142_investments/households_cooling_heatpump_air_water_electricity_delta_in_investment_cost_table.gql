# investment costs of households_cooling_heatpump_air_water_electricity IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(households_cooling_heatpump_air_water_electricity,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
