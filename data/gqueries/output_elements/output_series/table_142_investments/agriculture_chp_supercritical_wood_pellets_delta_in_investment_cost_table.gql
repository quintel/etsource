# investment costs of agriculture_chp_supercritical_wood_pellets IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(agriculture_chp_supercritical_wood_pellets,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
