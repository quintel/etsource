# investment costs of energy_chp_supercritical_waste_mix IF newly built (green field approach)
- unit = euro

QUERY_DELTA( -> { 
   V(energy_chp_supercritical_waste_mix,total_investment_over_lifetime_per(:converter)) 
}) / BILLIONS
