# investment costs of energy_power_wind_turbine_coastal IF newly built (green field approach)
- unit = euro

QUERY_DELTA( -> { 
   V(energy_power_wind_turbine_coastal,total_investment_over_lifetime_per(:converter)) 
}) / BILLIONS
