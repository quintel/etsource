# investment costs of other_chp_engine_biogas IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(other_chp_engine_biogas,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
