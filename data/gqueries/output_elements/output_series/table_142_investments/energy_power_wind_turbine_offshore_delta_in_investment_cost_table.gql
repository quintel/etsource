# investment costs of energy_power_wind_turbine_offshore IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(energy_power_wind_turbine_offshore,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
