# investment costs of households_water_heater_heatpump_ground_water_electricity IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(households_water_heater_heatpump_ground_water_electricity,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
