# investment costs of buildings_space_heater_coal IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(buildings_space_heater_coal,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
