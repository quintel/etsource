# investment costs of households_space_heater_combined_network_gas IF newly built (green field approach)
- unit = euro

QUERY_DELTA( -> { 
   V(households_space_heater_combined_network_gas,total_investment_over_lifetime_per(:converter)) 
}) / BILLIONS
