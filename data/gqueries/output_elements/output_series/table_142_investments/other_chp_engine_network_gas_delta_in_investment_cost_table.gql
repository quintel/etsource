# investment costs of other_chp_engine_network_gas IF newly built (green field approach)
- unit = euro

QUERY_DELTA( -> { 
   V(other_chp_engine_network_gas,total_investment_over_lifetime_per(:converter)) 
}) / BILLIONS
