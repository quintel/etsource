# Query for Sankey diagram: connection between buildings and useful_demand_transport

- query = future:DIVIDE(SUM(V(INTERSECTION(SECTOR(buildings),G(useful_demand_transport)),primary_demand)),BILLIONS)
- unit = pj
