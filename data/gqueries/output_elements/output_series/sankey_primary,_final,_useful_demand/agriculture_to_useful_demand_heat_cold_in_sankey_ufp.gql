# Query for Sankey diagram: connection between agriculture and useful_demand_heat_cold

- query = future:DIVIDE(SUM(V(INTERSECTION(SECTOR(agriculture),G(useful_demand_heat_cold)),primary_demand)),BILLIONS)
- unit = pj
