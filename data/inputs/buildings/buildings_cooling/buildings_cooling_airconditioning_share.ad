- factor = 100.0
- id = 393
- max_value = 100.0
- min_value = 0.0
- unit = %
- update_period = future
- step_value = 0.1
- share_group = cooling_buildings
- start_value_gql = present:V(buildings_cooling_airconditioning_electricity,share_of_buildings_useful_demand_after_insulation_recirculation_recovery_cooling)

EACH(
            UPDATE(LINK(buildings_cooling_airconditioning_electricity,buildings_useful_demand_after_insulation_recirculation_recovery_cooling), share, DIVIDE(USER_INPUT(),100)),
            UPDATE(LINK(buildings_useful_demand_after_insulation_recirculation_recovery_cooling,buildings_cooling_airconditioning_electricity), share,
              ->{SUM(NEG(SUM(V(EXCLUDE(INPUT_LINKS(V(buildings_useful_demand_after_insulation_recirculation_recovery_cooling)),UPDATE_COLLECTION()), share))), 1)}
            )
          )