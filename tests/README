Tests, at the moment used by developers, to verify if calculations did not change.

    # make sure you have uptodate environment
    # $ cap staging db2local
    # update git repositories
    # restart memcached

    rake etsource:update_tests GQUERIES='output_elements/**/*.gql'

    rspec spec/etsource

 If tests use scenarios, make sure to put them into scenarios/.