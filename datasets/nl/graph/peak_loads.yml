# Peak loads seem to be identical in all countries.
# Therefore it was moved to _defaults/graph/peak_loads.yml
# 
# To include peak loads uncomment the next line:
# To exclude peak loads comment the next line:
<<: *include_peak_loads