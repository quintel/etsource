- query =
    UPDATE(
      LINK(households_lighting_efficient_fluorescent_electricity, households_useful_demand_light),
      share,
      DIVIDE(USER_INPUT(), 100)
    )
- share_group = lighting_households
- priority = 1
