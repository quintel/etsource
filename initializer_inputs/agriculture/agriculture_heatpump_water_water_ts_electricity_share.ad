- query =
    UPDATE(
      LINK(agriculture_heatpump_water_water_ts_electricity, agriculture_useful_demand_useable_heat),
      share,
      DIVIDE(USER_INPUT(), 100)
    )
- share_group = agri_heat
- priority = 1
