- query =
    UPDATE(
      LINK(transport_car_using_compressed_natural_gas, transport_useful_demand_car_kms),
      share,
      DIVIDE(USER_INPUT(), 100)
    )
- share_group = transport_car_tech
- priority = 1
