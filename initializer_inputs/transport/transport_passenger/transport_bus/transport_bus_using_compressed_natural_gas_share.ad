- query =
    UPDATE(
      LINK(transport_bus_using_compressed_natural_gas, transport_useful_demand_busses),
      share,
      DIVIDE(USER_INPUT(), 100)
    )
- share_group = transport_bus_tech
- priority = 0
