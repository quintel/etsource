- query =
    UPDATE(
      LINK(transport_useful_demand_busses, transport_useful_demand_busses),
      share,
      DIVIDE(USER_INPUT(), 100)
    )
- share_group = passenger_transport
- priority = 0
