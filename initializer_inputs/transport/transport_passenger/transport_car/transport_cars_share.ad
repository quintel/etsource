- query =
    UPDATE(
      LINK(transport_useful_demand_cars, transport_useful_demand_passenger_kms),
      share,
      DIVIDE(USER_INPUT(), 100)
    )
- share_group = passenger_transport
- priority = 0

