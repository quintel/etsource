- query =
    UPDATE(
      LINK(transport_useful_demand_ships, transport_useful_demand_freight_tonne_kms),
      share,
      DIVIDE(USER_INPUT(), 100)
    )
- share_group = freight_transport
- priority = 0
