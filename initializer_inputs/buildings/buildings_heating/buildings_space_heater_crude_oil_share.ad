- query =
    UPDATE(
      LINK(buildings_space_heater_crude_oil, buildings_useful_demand_for_space_heating_after_insulation),
      share,
      DIVIDE(USER_INPUT(), 100)
    )
- share_group = heating_buildings
- priority = 1
