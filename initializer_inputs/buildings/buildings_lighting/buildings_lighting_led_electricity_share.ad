- query =
    UPDATE(
      LINK(buildings_lighting_led_electricity, buildings_useful_demand_after_motion_detection_daylight_control_light),
      share,
      DIVIDE(USER_INPUT(), 100)
    )
- share_group = lighting_buildings
- priority = 1
