- query =
    UPDATE(
      LINK(buildings_cooling_heatpump_air_water_network_gas, buildings_useful_demand_after_insulation_cooling),
      share,
      DIVIDE(USER_INPUT(), 100)
    )
- share_group = cooling_buildings
- priority = 1
