- query =
    UPDATE(
      V(OUTPUT_SLOTS(LOOKUP(buildings_chp_engine_biogas),steam_hot_water), "links.detect{|l| !l.flexible? }"),
      share, USER_INPUT() / 100.0
    )
- share_group = district_heating_buildings
- priority = 1
