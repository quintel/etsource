- query =
    UPDATE(
      FIRST(
        LINKS(
          OUTPUT_SLOTS(
            LOOKUP(buildings_heat_network_connection_steam_hot_water),
            steam_hot_water
          )
        )
      ),
      share, USER_INPUT() / 100.0
    )
- share_group = district_heating_buildings
- priority = 1
