- query =
    UPDATE(
      LINK(transport_bus_using_gasoline_mix, transport_useful_demand_busses),
      share,
      DIVIDE(USER_INPUT(), 100)
    )
- share_group = transport_bus_tech
- priority = 0
- max_value = 100.0
- min_value = 0.0
- start_value_gql = present:V(transport_bus_using_gasoline_mix,share_of_transport_useful_demand_busses) * 100
- step_value = 0.1
- unit = %
- update_period = future
