- query =
    EACH(
        UPDATE_WITH_FACTOR(AREA(), heat_lt_primary_pipelines_investment_costs_per_kw, USER_INPUT()),
        UPDATE_WITH_FACTOR(AREA(), heat_mt_primary_pipelines_investment_costs_per_kw, USER_INPUT()),
        UPDATE_WITH_FACTOR(AREA(), heat_ht_primary_pipelines_investment_costs_per_kw, USER_INPUT()),
        UPDATE_WITH_FACTOR(AREA(), heat_lt_distribution_pipelines_investment_costs_eur_per_meter, USER_INPUT()),
        UPDATE_WITH_FACTOR(AREA(), heat_mt_distribution_pipelines_investment_costs_eur_per_meter, USER_INPUT()),
        UPDATE_WITH_FACTOR(AREA(), heat_ht_distribution_pipelines_investment_costs_eur_per_meter, USER_INPUT()),
        UPDATE_WITH_FACTOR(AREA(), heat_lt_sub_station_investment_costs_eur_per_kw, USER_INPUT()),
        UPDATE_WITH_FACTOR(AREA(), heat_mt_sub_station_investment_costs_eur_per_kw, USER_INPUT()),
        UPDATE_WITH_FACTOR(AREA(), heat_ht_sub_station_investment_costs_eur_per_kw, USER_INPUT()),
        UPDATE_WITH_FACTOR(AREA(), heat_exchanger_station_investment_costs_eur_per_kw, USER_INPUT())
        )
- priority = 0
- max_value = 300.0
- min_value = 0
- start_value = 100.0
- step_value = 1.0
- unit = %
- update_period = future
- update_type = %
