---
comments: Brutal solution for the etflex slider groups updates issue. The single slider will update also the other members of the group. Ugly and incomplete
factor: 100.0
id: 1005
label: ""
label_query: ""
max_value: 100.0
max_value_gql:
min_value: 0.0
min_value_gql:
query: |-
  EACH(
    UPDATE(
      LINK(transport_useful_demand_car_kms, transport_car_using_electricity),
      share,
      DIVIDE(USER_INPUT(),100)
    ),
    UPDATE(
      LINK(transport_useful_demand_car_kms, transport_car_using_gasoline_mix),
      share,
      DIVIDE(SUM(100,NEG(USER_INPUT())), 200)
    ),
    UPDATE(
      LINK(transport_useful_demand_car_kms, transport_car_using_diesel_mix),
      share,
      DIVIDE(SUM(100,NEG(USER_INPUT())), 200)
    )
  )
share_group:
start_value: 0.0
start_value_gql: future:V( LINK(transport_useful_demand_car_kms, transport_car_using_electricity),share )
unit: "%"
update_period: future
update_type:
step_value: 0.1