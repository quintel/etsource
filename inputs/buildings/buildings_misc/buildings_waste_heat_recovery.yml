---
factor: 100.0
id: 397
label: ""
label_query:
max_value: 50.0
max_value_gql:
min_value: 0.0
min_value_gql:
query: |-
  EACH(
    UPDATE(FIRST(LINKS(OUTPUT_SLOTS(V(buildings_heating_savings_from_recovery_useable_heat),useable_heat))), share, DIVIDE(USER_INPUT(),V(100.0))),
    UPDATE(FIRST(LINKS(OUTPUT_SLOTS(V(buildings_cooling_savings_from_recovery_cooling),cooling))), share, DIVIDE(USER_INPUT(),V(100.0)))
  )
share_group: ""
start_value:
start_value_gql: present:AREA(heat_recovery)
unit: "%"
updateable_period: future
update_type:
