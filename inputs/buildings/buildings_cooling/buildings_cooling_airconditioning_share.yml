---
comments:
factor: 100.0
id: 393
label:
label_query:
max_value: 100.0
max_value_gql:
min_value: 0.0
min_value_gql:
query: |-
  EACH(
              UPDATE(LINK(buildings_cooling_airconditioning,buildings_useful_demand_after_insulation_recirculation_recovery_cooling), share, DIVIDE(USER_INPUT(),100)),
              UPDATE(LINK(buildings_useful_demand_after_insulation_recirculation_recovery_cooling,buildings_cooling_airconditioning), share,
                ->{SUM(NEG(SUM(V(EXCLUDE(INPUT_LINKS(V(buildings_useful_demand_after_insulation_recirculation_recovery_cooling)),UPDATE_COLLECTION()), share))), 1)}
              )
            )
share_group: cooling_buildings
start_value:
start_value_gql: present:V(buildings_cooling_airconditioning,share_of_buildings_useful_demand_after_insulation_recirculation_recovery_cooling)
unit: "%"
update_period: future
update_type:
