--- 
id: 395
factor: 1.0
key: buildings_ventilation_rate
name: Ventilation rate
max_value: 3.0
min_value: 1.5
start_value_gql: present:AREA(ventilation_rate)
query: |-
  EACH(
    UPDATE(V(heating_schools_current_insulation_buildings_energetic), preset_demand, -> { USER_INPUT() / AREA(ventilation_rate) * V(UPDATE_OBJECT(), preset_demand) }),
    UPDATE(V(cooling_demand_buildings_energetic),                     preset_demand, -> { USER_INPUT() / AREA(ventilation_rate) * V(UPDATE_OBJECT(), preset_demand) })
  )
unit: x
update_type: converters
updateable_period: future
v1_legacy_unit: 
