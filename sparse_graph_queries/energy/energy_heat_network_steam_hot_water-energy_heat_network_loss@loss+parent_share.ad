# this query calculates the share of loss in the "central" heat network based
# on demands in the parent dataset (parent_values.csv)

- query = PARENT_VALUE(energy_heat_network_loss, demand) / PARENT_VALUE(energy_heat_network_steam_hot_water, demand)
