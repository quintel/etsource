# User provides heat production, divide by heat output efficiency to get node demand

- query =
    DATASET_INPUT(input_energy_heat_burner_waste_mix_production) /
    ( DATASET_INPUT(energy_heat_burner_waste_mix_steam_hot_water_output_conversion) / 100.0 )
