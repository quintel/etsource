# sets input slot shares of mixer equal to the user input for different carriers coming into the node
# if demand of mixer is zero, calculate parent dataset default slot shares

- query =
    {network_gas: DATASET_INPUT(input_share_mixer_gas_fuel_network_gas),
    crude_oil: DATASET_INPUT(input_share_mixer_gas_fuel_oil),
    bio_oil: DATASET_INPUT(input_share_mixer_gas_fuel_bio_oil)}
