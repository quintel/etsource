# since there is no biogas import node, biogas production has to be at least as large as biogas use in CHPs
# plus the amount that gets upgraded to green gas

- query =
    SPARSE_GRAPH_QUERY(households_collective_chp_biogas, demand) +
    SPARSE_GRAPH_QUERY(buildings_chp_engine_biogas, demand) +
    DATASET_INPUT(agriculture_chp_engine_biogas_demand) +
    (DATASET_INPUT(input_energy_greengas_production) /
    NODE_EFFICIENCY(energy_upgrade_biogas, output, greengas))

