# this query caps the input heat demand reduction to the heat demand reduction
# corresponding to high insulation

- query = IF((100.0 * DATASET_INPUT(input_households_semi_detached_houses_heat_demand_reduction)) > AREA(heat_demand_reduction_high_insulation_semi_detached_house), AREA(heat_demand_reduction_high_insulation_semi_detached_house), 100.0 * DATASET_INPUT(input_households_semi_detached_houses_heat_demand_reduction))
