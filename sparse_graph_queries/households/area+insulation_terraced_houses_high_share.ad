# this query calculates the heat demand profile shares

- query =
    reduction = AREA(insulation_terraced_houses_start_value);
    medium_reduction = AREA(heat_demand_reduction_medium_insulation_terraced_house);
    high_reduction = AREA(heat_demand_reduction_high_insulation_terraced_house);

    IF(reduction >= medium_reduction, DIVIDE((reduction - medium_reduction), (high_reduction - medium_reduction)), 0.0)
