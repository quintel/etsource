# final demand electricity equals electricity from the grid (user input) plus own use of solar pv

- query =
      DATASET_INPUT(input_households_electricity_demand)
      + (DATASET_INPUT(input_households_solar_pv_demand) * DATASET_INPUT(input_households_solar_pv_own_use_share))
