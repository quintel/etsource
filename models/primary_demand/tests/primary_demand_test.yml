settings:
  country: nl
  
tests:
  setup_test_1: {query: 'COUNT(G(primary_energy_demand))',     present: 2}
  
  test_0: {query: 'ATTR(LOOKUP(lft),primary_demand)',                      present: 100}
  # primary_demand_of individual carrier
  test_1: {query: 'ATTR(LOOKUP(lft),primary_demand_of(CARRIER(oil)))',     present: 50}
  test_2: {query: 'ATTR(LOOKUP(lft),primary_demand_of(CARRIER(gas)))',     present: 50}
  # primary_demand_of accepts multiple carriers
  test_3: {query: 'ATTR(LOOKUP(lft),primary_demand_of(CARRIER(gas,oil)))', present: 100}