# The average gas per MJ electricity produced by gas plants, given in KG per MJ. This value can be used to determine the attribute of foreign_gas_electricity if exported or imported.

- unit = euro

DIVIDE(
   SUM(V(Q(electricity_producing_converters_gas);total_costs)),
   SUM(V(Q(electricity_producing_converters_gas);output_of_electricity))
)