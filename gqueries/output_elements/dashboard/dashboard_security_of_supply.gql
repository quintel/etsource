# Dashboard item for the number of expected blackout hours per year.

- query = V(GRAPH(), loss_of_load_probability(Q(available_electricity_production_capacity)))
- unit = hours
- deprecated_key = security_of_supply_blackout_risk
