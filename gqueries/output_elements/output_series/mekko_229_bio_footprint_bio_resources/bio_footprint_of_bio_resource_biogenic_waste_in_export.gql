# Bio-footprint of biogenic_waste in export

- unit = km2
- query =
    V(G(energy_export), demand_of_bio_resources_including_abroad_of(biogenic_waste)).sum / V(CARRIER(biogenic_waste), typical_production_per_km2)
