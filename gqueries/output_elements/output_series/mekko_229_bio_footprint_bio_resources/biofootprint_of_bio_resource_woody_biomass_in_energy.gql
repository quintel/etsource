# Bio-footprint of bio-resource woody_biomass in energy sector

- unit = km2
- query =
    Q(demand_of_bio_resource_woody_biomass_in_energy).sum / V(CARRIER(woody_biomass), typical_production_per_km2)
