# Used to be this
# stored:policy_onshore_coast
# 

- deprecated_key = chart_coastal_100_35_targeted_area_use_for_wind_turbines_300
- unit = km2

MIXED(
  Q(share_of_designated_area_used_for_coastal_wind),
  PRODUCT(
  DIVIDE(
    V(GOAL(onshore_coast),user_value),
    AREA(coast_line)
   ),100)
)
