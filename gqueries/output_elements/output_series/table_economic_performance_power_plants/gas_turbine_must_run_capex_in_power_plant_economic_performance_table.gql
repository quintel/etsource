# CAPEX of power plant, based on annual CAPEX calculations

- query =
   IF(
       V(energy_power_turbine_network_gas_must_run, output_of_electricity) == 0,
       -> { '-' },
       -> { V(energy_power_turbine_network_gas_must_run, capital_expenditures_per(:mwh_electricity)) }
   )
- unit = EUR/MWh
