- unit = PJ
- query =
    DIVIDE(
        SUM(
           V(
               CHILDREN(
                   V(bunkers_useful_demand_planes)
               ),demand
           )
        ), BILLIONS
    )
