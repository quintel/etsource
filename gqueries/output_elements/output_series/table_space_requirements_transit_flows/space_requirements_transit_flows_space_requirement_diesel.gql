# Future space requirements of diesel
# Returns future space requirements of imported diesel that is part of the transit flows in km^2. 

- query = 
    PRODUCT(
      V(energy_import_diesel,land_use_per_unit),
      V(energy_import_diesel,number_of_units)
    )
- unit = km2
