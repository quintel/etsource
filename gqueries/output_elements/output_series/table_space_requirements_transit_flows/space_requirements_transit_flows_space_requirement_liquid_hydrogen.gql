# Future space requirements of liquid hydrogen
# Returns future space requirements of imported liquid hydrogen that is part of the transit flows in m2.

- query = 
    PRODUCT(
      V(energy_import_liquid_hydrogen,land_use_per_unit),
      V(energy_import_liquid_hydrogen,number_of_units)
    ) * MILLIONS
- unit = m2
