# Bio-footprint of bio_ethanol in agriculture sector

- unit = km2
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(agriculture)), primary_demand_of_bio_ethanol
    ).sum / V(CARRIER(bio_ethanol), typical_production_per_km2)
