# Bio-footprint of biodiesel in transport sector

- unit = km2
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(transport)), primary_demand_of_biodiesel
    ).sum / V(CARRIER(biodiesel), typical_production_per_km2)
