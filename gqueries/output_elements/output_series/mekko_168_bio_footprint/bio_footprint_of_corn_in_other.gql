# Bio-footprint of corn in other sector

- unit = km2
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(other)), primary_demand_of_corn
    ).sum / V(CARRIER(corn), typical_production_per_km2)
