# Bio-footprint of greengas in buildings sector

- unit = km2
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(buildings)), primary_demand_of_greengas
    ).sum / V(CARRIER(greengas), typical_production_per_km2)
