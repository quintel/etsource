# Bio-footprint of biodiesel in buildings sector

- unit = km2
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(buildings)), primary_demand_of_biodiesel
    ).sum / V(CARRIER(biodiesel), typical_production_per_km2)
