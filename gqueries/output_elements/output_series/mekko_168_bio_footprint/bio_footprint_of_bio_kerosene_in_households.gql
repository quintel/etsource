# Bio-footprint of bio_kerosene in households sector

- unit = km2
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(households)), primary_demand_of_bio_kerosene
    ).sum / V(CARRIER(bio_kerosene), typical_production_per_km2)
