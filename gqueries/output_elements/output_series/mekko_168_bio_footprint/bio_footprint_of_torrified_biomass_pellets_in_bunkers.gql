# Bio-footprint of torrified_biomass_pellets in bunkers sector

- unit = km2
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(bunkers)), primary_demand_of_torrified_biomass_pellets
    ).sum / V(CARRIER(torrified_biomass_pellets), typical_production_per_km2)
