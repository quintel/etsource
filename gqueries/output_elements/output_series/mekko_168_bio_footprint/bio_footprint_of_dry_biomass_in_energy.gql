# Bio-footprint of dry_biomass in energy sector

- unit = km2
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(energy)), primary_demand_of_dry_biomass
    ).sum / V(CARRIER(dry_biomass), typical_production_per_km2)
