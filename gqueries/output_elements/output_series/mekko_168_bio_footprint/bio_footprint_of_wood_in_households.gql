# Bio-footprint of wood in households sector

- unit = km2
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(households)), primary_demand_of_wood
    ).sum / V(CARRIER(wood), typical_production_per_km2)
