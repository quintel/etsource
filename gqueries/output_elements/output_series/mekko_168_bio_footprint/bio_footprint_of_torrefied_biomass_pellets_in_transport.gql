# Bio-footprint of torrefied_biomass_pellets in transport sector

- unit = km2
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(transport)), primary_demand_of_torrefied_biomass_pellets
    ).sum / V(CARRIER(torrefied_biomass_pellets), typical_production_per_km2)
