# Bio-footprint of dry_biomass in transport sector

- unit = km2
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(transport)), primary_demand_of_dry_biomass
    ).sum / V(CARRIER(dry_biomass), typical_production_per_km2)
