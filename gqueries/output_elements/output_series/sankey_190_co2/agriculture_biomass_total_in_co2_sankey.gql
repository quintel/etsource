# CO2 of biomass in agriculture

- unit = tonne
- query =
    PRODUCT(
        V(agriculture_final_demand_wood_pellets, demand),
        V(CARRIER(wood_pellets), potential_co2_conversion_per_mj)
    ) / THOUSANDS
