# CO2 emissions in agriculture

- unit = tonne
- query =
    SUM(
        Q(agriculture_natural_gas_total_in_co2_sankey),
        Q(agriculture_oil_total_in_co2_sankey),
        Q(agriculture_biomass_total_in_co2_sankey)
    )
