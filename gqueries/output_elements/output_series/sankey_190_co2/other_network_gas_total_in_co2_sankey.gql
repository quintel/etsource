# CO2 of network gas (green + fossil) in other

- unit = tonne
- query =
    PRODUCT(
        V(other_final_demand_network_gas, demand),
        V(CARRIER(natural_gas), co2_conversion_per_mj)
    ) / THOUSANDS
