# Outputs the nodes that generate heat from waste water for lt and mt heat networks

- query =
    INTERSECTION(
        PARENTS(V(energy_production_waste_water_ambient_heat)),
        SECTOR(energy)
    )
- unit = nodes