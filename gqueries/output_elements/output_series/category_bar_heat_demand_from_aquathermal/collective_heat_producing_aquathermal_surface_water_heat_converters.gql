# Outputs the nodes that generate heat from surface water for lt and mt heat networks

- query =
    INTERSECTION(
        PARENTS(V(energy_production_surface_water_ambient_heat)),
        SECTOR(energy)
    )
- unit = nodes