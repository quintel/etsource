- deprecated_key = chart_cooling_from_gas_66_cooling_demand_594
# 

- unit = pj

DIVIDE(PRODUCT(DIVIDE(Q(households_cooling_gas),SUM(V(Q(households_cooling_converters);demand))),V(cooling_demand_households_energetic;input_of_cooling)),BILLIONS)