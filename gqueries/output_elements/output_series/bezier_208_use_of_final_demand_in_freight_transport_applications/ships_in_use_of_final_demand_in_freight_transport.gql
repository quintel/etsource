- unit = PJ
- query =
    DIVIDE(
        SUM(
           V(
               CHILDREN(
                   V(transport_useful_demand_ships)
               ),demand
           )
        ), BILLIONS
    )
