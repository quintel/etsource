# investment costs of energy_heat_burner_coal IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(energy_heat_burner_coal,total_investment_over_lifetime_per(:converter))
    }) / BILLIONS
- unit = euro
