# investment costs of other_burner_coal IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(other_burner_coal,total_investment_over_lifetime_per(:node))
    }) / BILLIONS
- unit = euro
