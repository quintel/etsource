# investment costs of energy_greengas_gasification_wet_biomass IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(energy_greengas_gasification_wet_biomass,total_investment_over_lifetime_per(:converter))
    }) / BILLIONS
- unit = euro
