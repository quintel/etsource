# investment costs of energy_chp_supercritical_ccs_waste_mix IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(energy_chp_supercritical_ccs_waste_mix,total_investment_over_lifetime_per(:node))
    }) / BILLIONS
- unit = euro
