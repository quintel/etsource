# investment costs of energy_power_hydro_river IF newly built (green field approach)

- query = present:V(energy_power_hydro_river, total_investment_over_lifetime_per(:node)) / BILLIONS
- unit = euro
