# investment costs of energy_power_turbine_network_gas_must_run IF newly built (green field approach)

- query = future:V(energy_power_turbine_network_gas_must_run, total_investment_over_lifetime_per(:node)) / BILLIONS
- unit = euro
