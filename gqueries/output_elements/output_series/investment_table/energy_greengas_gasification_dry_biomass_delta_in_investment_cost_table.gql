# investment costs of energy_greengas_gasification_dry_biomass IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(energy_greengas_gasification_dry_biomass,total_investment_over_lifetime_per(:node))
    }) / BILLIONS
- unit = euro
