# investment costs of energy_power_supercritical_ccs_waste_mix IF newly built (green field approach)

- query = present:V(energy_power_supercritical_ccs_waste_mix, total_investment_over_lifetime_per(:node)) / BILLIONS
- unit = euro
