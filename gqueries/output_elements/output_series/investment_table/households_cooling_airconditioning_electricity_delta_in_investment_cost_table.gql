# investment costs of households_cooling_airconditioning_electricity IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(households_cooling_airconditioning_electricity,total_investment_over_lifetime_per(:node))
    }) / BILLIONS
- unit = euro
