# investment costs of energy_heat_well_geothermal IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(energy_heat_well_geothermal,total_investment_over_lifetime_per(:converter))
    }) / BILLIONS
- unit = euro
