# investment costs of energy_flexibility_mv_batteries_electricity IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(energy_flexibility_mv_batteries_electricity,total_investment_over_lifetime_per(:converter))
    }) / BILLIONS
- unit = euro
