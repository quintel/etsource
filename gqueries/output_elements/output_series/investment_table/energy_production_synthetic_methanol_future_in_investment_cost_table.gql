# investment costs of energy_production_methanol_synthesis_synthetic IF newly built (green field approach)

- query = future:V(energy_production_methanol_synthesis_synthetic, total_investment_over_lifetime_per(:node)) / BILLIONS
- unit = euro
