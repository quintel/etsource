# investment costs of industry_other_food_flexibility_p2h_network_gas_electricity IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(industry_other_food_flexibility_p2h_network_gas_electricity,total_investment_over_lifetime_per(:node))
    }) / BILLIONS
- unit = euro
