# investment costs of energy_chp_local_engine_network_gas IF newly built (green field approach)

- query = present:V(energy_chp_local_engine_network_gas, total_investment_over_lifetime_per(:node)) / BILLIONS
- unit = euro
