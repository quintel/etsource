# investment costs of industry_other_paper_burner_hydrogen IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(industry_other_paper_burner_hydrogen,total_investment_over_lifetime_per(:node))
    }) / BILLIONS
- unit = euro
