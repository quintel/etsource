# investment costs of energy_production_fischer_tropsch_synthetic_must_run IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(energy_production_fischer_tropsch_synthetic_must_run,total_investment_over_lifetime_per(:node))
    }) / BILLIONS
- unit = euro
