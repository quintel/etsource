- query =
          IF(
              GRAPH().present?,
              -> { 0 },
              -> { QUERY_FUTURE(interconnection_net_costs_delta_present_future_investment_costs) }
          )

- unit = euro
