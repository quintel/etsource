- query =
    DIVIDE(PRODUCT(
     V(households_final_demand_steam_hot_water,demand),
     SUM(
      1,
      NEG(V(households_final_demand_steam_hot_water,sustainability_share)
      )
     )
    ),BILLIONS)
- unit = mj
