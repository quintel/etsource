- unit = PJ
- query =
    DIVIDE(
      PRODUCT(
        V(bunkers_final_demand_for_aviation_kerosene,demand),
        SUM(
          V(EDGE(energy_distribution_kerosene, energy_production_fischer_tropsch_synthetic_dispatchable), share),
          V(EDGE(energy_distribution_kerosene, energy_production_fischer_tropsch_synthetic_must_run), share)
        )
      ),
      BILLIONS
    )
