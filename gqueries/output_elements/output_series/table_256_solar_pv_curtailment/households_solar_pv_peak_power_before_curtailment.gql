# Peak power before curtailment of 'households_solar_pv_solar_radiation'

- unit = MW
- query =
    SUM(
      MAX(Q(households_solar_pv_electricity_output_curve)),
      MAX(Q(households_solar_pv_curtailed_electricity_output_curve))
    )
