# Query for Sankey diagram: connection between hydrogen_production and energy

- unit = PJ
- query = DIVIDE(SUM(V(INTERSECTION(INTERSECTION(SECTOR(energy),G(final_demand_group)), USE(energetic)), "input_of_hydrogen")), BILLIONS)
