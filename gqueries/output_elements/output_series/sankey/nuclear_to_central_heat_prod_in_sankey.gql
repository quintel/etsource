# Query for Sankey diagram: connection between nuclear and central heat & CHPs

- unit = PJ
- query = DIVIDE(SUM(V(Q(central_heat_producing_converters_sankey), primary_demand_of(uranium_oxide))),BILLIONS)
