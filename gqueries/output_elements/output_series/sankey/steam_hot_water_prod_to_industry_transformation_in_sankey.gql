# Query direct conversion demand of steam_hot_water for industry transformation

- query =
    DIVIDE(
      SUM(
        V(
          G(industry_transformation),
          input_of_steam_hot_water
        )
      ),
      BILLIONS
    )
- unit = PJ
