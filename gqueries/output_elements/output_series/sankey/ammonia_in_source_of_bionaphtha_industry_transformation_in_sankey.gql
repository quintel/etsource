# Query for Sankey diagram: connection between bionaphtha from ammonia in industry transformation

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          G(industry_transformation),
          "output_of_bionaphtha * ammonia_input_conversion"
        )
      ),
      BILLIONS
    )
