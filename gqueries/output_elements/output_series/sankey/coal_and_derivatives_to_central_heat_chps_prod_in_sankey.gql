# Query for Sankey diagram: connection between coal_and_derivatives and chps

- unit = PJ
- query = DIVIDE(SUM(V(Q(heat_producing_converters_sankey), primary_demand_of(coal , lignite, cokes, coal_gas))),BILLIONS)
