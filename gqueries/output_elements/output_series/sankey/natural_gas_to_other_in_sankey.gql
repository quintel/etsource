# Query for Sankey download: connection between natural_gas and other

- unit = PJ
- query = 
    SUM(
      SUM(
        V(
          FILTER(
            FILTER(
              FILTER(
                EG(final_demand),"sector == :other"), "natural_gas? || lng? || propane?"
            ),
            "energetic?"
          ),
          value
        )
      ),
      PRODUCT(
        SUM(
          SUM(
            V(
              FILTER(
                FILTER( 
                  FILTER(
                    EG(final_demand),"sector == :other"), "network_gas?"
                ),
                "energetic?"
              ),
              value
            )
          ),
          PRODUCT(
            SUM(
              V(
                FILTER(
                  FILTER(
                    FILTER(
                      EG(final_demand),"sector == :other"), "compressed_network_gas?"
                  ),
                  "energetic?"
                ),
                value
              )
            ),
            Q(share_of_network_gas_in_compressed_network_gas)
          )
        ),
        Q(share_of_fossil_gas_in_gas_network)
      )
    ) / BILLIONS
