# Query for Sankey diagram: connection between chps (electricity) and transport

- query = DIVIDE(SUM(V(INTERSECTION(SECTOR(transport),G(final_demand_group)),"input_of_electricity")),BILLIONS)*(Q(fraction_of_electricity_chps_in_sankey))
- unit = pj
