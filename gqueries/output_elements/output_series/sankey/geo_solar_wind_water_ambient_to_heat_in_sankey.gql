# Query for Sankey diagram: connection between geo_solar_wind_water_ambient and heat

- unit = pj

future:DIVIDE(SUM(V(Q(heat_producing_converters), primary_demand_of(Q(carrier_group_geo_solar_wind_water_ambient)))),BILLIONS)
		