# Query for Sankey diagram: connection between geo_solar_wind_water_ambient and heat

- unit = pj

future:DIVIDE(SUM(V(G(heat_production), primary_demand_of(Q(carrier_group_geo_solar_wind_water_ambient)))),BILLIONS)
		