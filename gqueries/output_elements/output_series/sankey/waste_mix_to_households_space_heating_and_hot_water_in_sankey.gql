# Query for Sankey diagram: connection between waste_mix and households

- unit = PJ
- query = 
    SUM(
      Q(final_demand_of_waste_mix_in_households_space_heating_energetic),
      Q(final_demand_of_waste_mix_in_households_hot_water_energetic)
    )

