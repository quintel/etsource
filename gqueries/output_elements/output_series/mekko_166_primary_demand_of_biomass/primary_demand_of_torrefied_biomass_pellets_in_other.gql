# Use of torrefied_biomass_pellets in other sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(other)), primary_demand_of_torrefied_biomass_pellets
    ).sum / BILLIONS
