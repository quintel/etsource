# Use of torrefied_biomass_pellets in industry sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(industry)), primary_demand_of_torrefied_biomass_pellets
    ).sum / BILLIONS
