- deprecated_key = chart_other_heat_62_decentral_production_572
#

- unit = pj

DIVIDE(
    MAX(
        SUM(
            V(agriculture_useful_demand_useable_heat,demand),
            NEG(
                V(agriculture_chp_engine_gas_power_fuelmix,output_of_heat_carriers)
            )
        ),
        0
    ),
    BILLIONS
)
