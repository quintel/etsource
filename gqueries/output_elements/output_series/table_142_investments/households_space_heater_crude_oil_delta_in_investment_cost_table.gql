# investment costs of households_space_heater_crude_oil IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(households_space_heater_crude_oil,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
