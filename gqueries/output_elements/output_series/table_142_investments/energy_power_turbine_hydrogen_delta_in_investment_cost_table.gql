# investment costs of energy_power_turbine_hydrogen 

- query =
    QUERY_DELTA( -> {
       V(energy_power_turbine_hydrogen,total_investment_over_lifetime_per(:converter))
    }) / BILLIONS
- unit = euro
