# investment costs of agriculture_chp_engine_network_gas IF newly built (green field approach)

- query = present:V(agriculture_chp_engine_network_gas, total_investment_over_lifetime_per(:converter)) / BILLIONS
- unit = euro
