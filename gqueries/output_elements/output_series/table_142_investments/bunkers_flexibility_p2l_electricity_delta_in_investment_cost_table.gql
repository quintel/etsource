# investment costs of bunkers_flexibility_p2l_electricity IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(bunkers_flexibility_p2l_electricity,total_investment_over_lifetime_per(:converter))
    }) / BILLIONS
- unit = euro
