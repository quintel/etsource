# investment costs of buildings_space_heater_electricity IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(buildings_space_heater_electricity,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
