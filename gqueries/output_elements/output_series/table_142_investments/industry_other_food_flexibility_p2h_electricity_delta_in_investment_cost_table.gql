# investment costs of industry_other_food_flexibility_p2h_electricity IF newly built (green field approach)

- query =
    QUERY_DELTA( -> {
       V(industry_other_food_flexibility_p2h_electricity,total_investment_over_lifetime_per(:converter))
    }) / BILLIONS
- unit = euro
