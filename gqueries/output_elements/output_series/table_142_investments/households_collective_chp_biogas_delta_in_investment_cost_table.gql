# investment costs of households_collective_chp_biogas IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(households_collective_chp_biogas,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
