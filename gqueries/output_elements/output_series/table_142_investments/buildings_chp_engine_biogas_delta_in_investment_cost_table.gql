# investment costs of buildings_chp_engine_biogas IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(buildings_chp_engine_biogas,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
