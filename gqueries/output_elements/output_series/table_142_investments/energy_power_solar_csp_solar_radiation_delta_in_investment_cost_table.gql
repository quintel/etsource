# investment costs of energy_power_solar_csp_solar_radiation IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(energy_power_solar_csp_solar_radiation,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
