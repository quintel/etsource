# investment costs of households_flexibility_p2p_electricity IF newly built (green field approach)

- query =
    QUERY_DELTA( -> { 
       V(households_flexibility_p2p_electricity,total_investment_over_lifetime_per(:converter)) 
    }) / BILLIONS
- unit = euro
