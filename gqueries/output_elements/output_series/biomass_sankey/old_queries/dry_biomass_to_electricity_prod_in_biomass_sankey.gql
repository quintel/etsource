# Query for Biomass Sankey diagram: connection between dry biomass and electricity (incl. chp)

- unit = PJ
- query =
     DIVIDE(
      SUM(V(G(electricity_production), input_of(wood_pellets, torrefied_biomass_pellets))),
      BILLIONS
      )
