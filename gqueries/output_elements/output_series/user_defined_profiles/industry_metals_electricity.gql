- query = 
    SUM_CURVES(
      V(
        FILTER(
        ALL(),
        "merit_order && (merit_order.group == :industry_metals_electricity)"
        ), 
        electricity_input_curve
       )
     )

- unit = curve