- query = 
    SUM_CURVES(
      V(
        FILTER(
        ALL(),
        "merit_order && (merit_order.group == :industry_other_electricity)"
        ), 
        electricity_input_curve
       )
     )

- unit = curve