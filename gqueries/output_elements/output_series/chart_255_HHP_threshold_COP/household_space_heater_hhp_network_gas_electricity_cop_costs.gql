# This query calculates the costs for one MJ heat when the HHP would
# use electricity with a COP of 1.

- query =
    DIVIDE(
      INPUT_VALUE(households_flexibility_consumer_electricity_price),
      MJ_PER_KWH
    )

- unit = euro
