# Query for Sankey diagram: connection between nuclear and transport

- unit = PJ
- query = future:DIVIDE(SUM(V(INTERSECTION(SECTOR(transport),G(final_demand_group)),primary_demand_of(Q(carrier_group_nuclear)))),BILLIONS)
