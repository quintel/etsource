# Query for Sankey diagram: connection between buildings and useful_demand_heat_cold

- query = future:DIVIDE(SUM(V(INTERSECTION(SECTOR(buildings),G(useful_demand_heat_cold)),primary_demand)),BILLIONS)
- unit = pj
