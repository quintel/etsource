# Query for Sankey diagram: connection between other and useful_demand_heat_cold

- query = future:DIVIDE(SUM(V(INTERSECTION(SECTOR(other),G(useful_demand_heat_cold)),primary_demand)),BILLIONS)
- unit = pj
