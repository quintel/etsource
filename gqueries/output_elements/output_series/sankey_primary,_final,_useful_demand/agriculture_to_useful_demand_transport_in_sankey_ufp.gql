# Query for Sankey diagram: connection between agriculture and useful_demand_transport

- unit = PJ
- query = future:DIVIDE(SUM(V(INTERSECTION(SECTOR(agriculture),G(useful_demand_transport)),primary_demand)),BILLIONS)
