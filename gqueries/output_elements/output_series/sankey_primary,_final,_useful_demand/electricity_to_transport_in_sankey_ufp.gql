# Query for Sankey diagram: connection between electricity and transport

- query = future:DIVIDE(SUM(V(INTERSECTION(SECTOR(transport),G(final_demand_group)),primary_demand_of(Q(carrier_group_electricity)))),BILLIONS)
- unit = pj
