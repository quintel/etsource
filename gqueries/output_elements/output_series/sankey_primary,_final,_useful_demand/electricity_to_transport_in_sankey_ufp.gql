# Query for Sankey diagram: connection between electricity and transport

- unit = pj

DIVIDE(SUM(V(INTERSECTION(SECTOR(transport),G(final_demand_cbs)),primary_demand_of(Q(carrier_group_electricity)))),BILLIONS)
			