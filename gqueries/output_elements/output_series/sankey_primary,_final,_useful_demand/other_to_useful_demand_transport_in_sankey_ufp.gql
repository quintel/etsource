# Query for Sankey diagram: connection between other and useful_demand_transport

- unit = PJ
- query = future:DIVIDE(SUM(V(INTERSECTION(SECTOR(other),G(useful_demand_transport)),primary_demand)),BILLIONS)
