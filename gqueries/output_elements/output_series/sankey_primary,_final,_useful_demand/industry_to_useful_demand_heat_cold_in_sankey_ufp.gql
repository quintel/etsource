# Query for Sankey diagram: connection between industry and useful_demand_heat_cold

- unit = PJ
- query = future:DIVIDE(SUM(V(INTERSECTION(SECTOR(industry),G(useful_demand_heat_cold)),primary_demand)),BILLIONS)
