# Query for Sankey diagram: connection between transport and useful_demand_heat_cold

- unit = PJ
- query = future:DIVIDE(SUM(V(INTERSECTION(SECTOR(transport),G(useful_demand_heat_cold)),primary_demand)),BILLIONS)
