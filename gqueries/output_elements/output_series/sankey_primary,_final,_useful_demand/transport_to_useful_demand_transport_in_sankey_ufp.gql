# Query for Sankey diagram: connection between transport and useful_demand_transport

- unit = pj

future:DIVIDE(SUM(V(INTERSECTION(SECTOR(transport),G(useful_demand_transport)),primary_demand)),BILLIONS)
