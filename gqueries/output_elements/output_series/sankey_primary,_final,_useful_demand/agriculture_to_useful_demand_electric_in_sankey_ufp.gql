# Query for Sankey diagram: connection between agriculture and useful_demand_electric

- unit = pj

DIVIDE(SUM(V(INTERSECTION(SECTOR(agriculture),G(useful_demand_electric)),primary_demand)),BILLIONS)
		