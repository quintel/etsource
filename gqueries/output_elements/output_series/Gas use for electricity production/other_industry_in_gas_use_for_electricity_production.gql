# 

- unit = pj

DIVIDE(
    SUM(
        V(INTERSECTION(SECTOR(industry),G(electricity_production)),"primary_demand - primary_demand_of_natural_gas - primary_demand_of_greengas - primary_demand_of_corn - primary_demand_of_manure")
    ),
    BILLIONS
)
