# 

- unit = pj

DIVIDE(
    SUM(
        V(INTERSECTION(SECTOR(energy),G(electricity_production)),"primary_demand_of_greengas + primary_demand_of_corn + primary_demand_of_manure")
    ),
    BILLIONS
)