# Query for Sankey diagram: connection between households and useful_demand_non_energetic

- unit = pj

future:DIVIDE(SUM(V(INTERSECTION(SECTOR(households),G(useful_demand_non_energetic)),demand)),BILLIONS)
		