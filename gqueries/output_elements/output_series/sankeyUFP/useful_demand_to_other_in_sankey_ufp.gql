# Query for Sankey diagram: connection between useful_demand and other

- unit = pj

future:DIVIDE(SUM(V(INTERSECTION(SECTOR(other),G(final_demand_cbs)),input_of(Q(carrier_group_useful_demand)))),BILLIONS)
			