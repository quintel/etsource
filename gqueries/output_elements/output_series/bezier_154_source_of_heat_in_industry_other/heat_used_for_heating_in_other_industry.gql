# Use of carrier group 'heat' in other industry heat

- unit = PJ
- query =
    SUM(
      V(CHILDREN(V(industry_useful_demand_useable_heat)),input_of_steam_hot_water),
      V(CHILDREN(V(industry_useful_demand_useable_heat)),input_of_useable_heat)
    ) / BILLIONS
