# Query for Sankey diagram: connection between transport and useful_demand_electric

- unit = pj

future:DIVIDE(SUM(V(INTERSECTION(SECTOR(transport),G(useful_demand_electric)),primary_demand)),BILLIONS)
		