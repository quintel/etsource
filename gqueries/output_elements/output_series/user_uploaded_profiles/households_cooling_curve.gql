- query =
    SUM_CURVES(
      V(
        FILTER(
          ALL(),
          "merit_order && (merit_order.group == :households_cooling)"
        ),
        "electricity_input_curve"
      )
    )

- unit = curve
