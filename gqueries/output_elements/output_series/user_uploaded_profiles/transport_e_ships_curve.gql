- query = 
    SUM_CURVES(
      V(
        FILTER(
        ALL(),
        "merit_order && (merit_order.group == :electric_ships)"
        ), 
        electricity_input_curve
       )
     )
     
- unit = curve