- query = 
    SUM_CURVES(
      V(
        FILTER(
        ALL(),
        "merit_order && (merit_order.group == :industry_chemicals_electricity)"
        ), 
        electricity_input_curve
       )
     )

- unit = curve