- query =
    SUM_CURVES(
      V(
        FILTER(
          ALL(),
          "merit_order && (merit_order.group == :buildings_appliances)"
        ),
        "electricity_input_curve"
      )
    )

- unit = curve
