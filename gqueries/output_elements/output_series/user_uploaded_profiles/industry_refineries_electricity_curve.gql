- query = 
    SUM_CURVES(
      V(
        FILTER(
        ALL(),
        "merit_order && (merit_order.group == :industry_refineries_electricity)"
        ), 
        electricity_input_curve
       )
     )

- unit = curve