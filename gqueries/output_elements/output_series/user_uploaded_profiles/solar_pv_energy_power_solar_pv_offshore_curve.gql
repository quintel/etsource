- query =
    SUM_CURVES(
      V(
        FILTER(
        ALL(),
        "merit_order && (merit_order.group.to_s.gsub(/\s/, '') == 'dynamic:solar_pv_offshore')"
        ),
        electricity_output_curve
       )
     )

- unit = curve
