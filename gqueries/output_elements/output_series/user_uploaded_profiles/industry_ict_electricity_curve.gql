- query = 
    SUM_CURVES(
      V(
        FILTER(
        ALL(),
        "merit_order && (merit_order.group == :industry_ict)"
        ), 
        electricity_input_curve
       )
     )

- unit = curve