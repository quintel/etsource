- deprecated_key = chart_solar_panels_51_centrally_produced_electricity_demand_407
# 

- unit = pj

SUM(
    QUERY_FUTURE( -> { NEG(DIVIDE(SUM(V(households_solar_pv_solar_radiation,buildings_solar_pv_solar_radiation,output_of_electricity)),BILLIONS)) } ),
    NEG(
        QUERY_PRESENT( -> { NEG(DIVIDE(SUM(V(households_solar_pv_solar_radiation,buildings_solar_pv_solar_radiation,output_of_electricity)),BILLIONS)) } )
    )
)