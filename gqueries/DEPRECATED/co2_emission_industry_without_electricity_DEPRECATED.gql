- deprecated_key = co2_emission_industry_without_electricity
# 

- unit = kg

   SUM(V (
   INTERSECTION(
        USE(energetic),
        INTERSECTION(SECTOR(industry),GROUP(final_demand_cbs) )
        )
       ,primary_co2_emission)
      ,
NEG (

        V( industry_final_demand_electricity
      ,primary_co2_emission))

)
