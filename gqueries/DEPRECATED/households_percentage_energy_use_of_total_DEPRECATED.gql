- deprecated_key = households_percentage_energy_use_of_total
# 

- unit = factor

DIVIDE(
SUM(V(INTERSECTION(G(final_demand_cbs),SECTOR(households));final_demand)),
SUM(V(G(final_demand_cbs);final_demand))
)