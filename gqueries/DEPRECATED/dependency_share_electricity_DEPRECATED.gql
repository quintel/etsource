# 

- unit = factor

DIVIDE(  
            SUM(            
                    V(electricity_import_export_energy;demand)
                  , V(electricity_export_energy;demand)),
             SUM(V(G(primary_energy_demand);demand)))
