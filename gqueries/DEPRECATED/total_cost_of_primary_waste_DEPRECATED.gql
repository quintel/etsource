# Waste has a determined share of sustainability.

- deprecated_key = total_cost_of_primary_waste
- unit = euro

PRODUCT(
  SUM(
    V(G(final_demand_cbs); primary_demand_of_waste_mix)
  )
  ,
  V(CARRIER(waste_mix);cost_per_mj * sustainable)
)