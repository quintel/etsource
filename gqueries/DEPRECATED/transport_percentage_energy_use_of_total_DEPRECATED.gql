- deprecated_key = transport_percentage_energy_use_of_total
# 

- unit = factor

DIVIDE(
SUM(V(INTERSECTION(G(final_demand_cbs),SECTOR(transport));final_demand)),
SUM(V(G(final_demand_cbs);final_demand))
)