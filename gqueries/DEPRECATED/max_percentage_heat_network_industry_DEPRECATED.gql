- deprecated_key = max_percentage_heat_network_industry
# The maximum share the heat network can supply of the industry heat demand, capped by 100%
# 
# OLD:
# MIN(
#   1,
#   DIVIDE(
#      V(industry_final_demand_steam_hot_water,demand)
#      ,
#      V(industry_useful_demand_useable_heat,demand)
#      )
#    )

- unit = factor

MIN(
  1,
  DIVIDE(
     SUM(V(industry_chp_combined_cycle_gas_power_fuelmix,energy_chp_combined_cycle_bio_oil,industry_chp_supercritical_wood_pellets,output_of_steam_hot_water))
     ,
     V(industry_useful_demand_useable_heat,demand)
     )
   )
