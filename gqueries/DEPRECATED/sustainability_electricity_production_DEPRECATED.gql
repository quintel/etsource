- deprecated_key = sustainability_electricity_production
# 

- unit = mj

DIVIDE(
   SUM(V( G(electricity_production);" useful_output * demand * sustainable_input_factor")),
   SUM(V( G(electricity_production);" useful_output * demand"))
)