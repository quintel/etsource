#

- unit = euro

future:DIVIDE(
    SUM(
        PRODUCT(
            Q(additional_backup_capacity_required_in_mw),
            V(coal_conv_energy ," fixed_costs_per_mw_input / electricity_output_conversion / availability")
        ),
        PRODUCT(
            Q(additional_spinning_reserve_required_in_mw),
            V(coal_conv_energy ," part_load_operating_point * HOURS_PER_YEAR / (1 - part_load_operating_point) * variable_costs_per_mwh_input * (FLOAT_HACK/(electricity_output_conversion - part_load_efficiency_penalty)-FLOAT_HACK/electricity_output_conversion)")
        )
    ),
    Q(volatile_electricity_production)
)