# The capacity of the low voltage net in the future.

- query = QUERY_PRESENT(lv_mv_trafo_capacity_present) + QUERY_FUTURE(lv_mv_trafo_capacity_steps_built) * AREA(lv_mv_trafo_capacity_per_step)

- unit = MW
