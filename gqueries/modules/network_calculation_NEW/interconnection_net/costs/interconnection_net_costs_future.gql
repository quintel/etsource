# The total costs of the network calculation

- query =
          SUM(
              QUERY_PRESENT(interconnection_net_costs_present),
              QUERY_FUTURE(interconnection_net_costs_delta_present_future)
          )

- unit = euro
