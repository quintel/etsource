# Energetic final demand of the 'natural_gas' carrier group in buildings space_heating

- unit = PJ
- query =
    PRODUCT(
      SUM(
        SUM(
          V(
            FILTER(
              FILTER(
                INTERSECTION(EG(final_demand),EG(space_heating_buildings)), "network_gas?"
              ),
              "energetic?"
            ),
            value
          )
        ),
        PRODUCT(
          SUM(
            V(
              FILTER(
                FILTER(
                  INTERSECTION(EG(final_demand),EG(space_heating_buildings)), "compressed_network_gas?"
                ),
                "energetic?"
              ),
              value
            )
          ),
          Q(share_of_network_gas_in_compressed_network_gas)
        )
      ),
      Q(share_of_fossil_gas_in_gas_network)
    ) / BILLIONS
