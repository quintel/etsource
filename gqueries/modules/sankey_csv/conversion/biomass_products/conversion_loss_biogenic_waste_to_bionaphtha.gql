- query =
    DIVIDE(
      SUM(
        V(
          FILTER(G(sankey_conversion_group),"bionaphtha_output_conversion > 0.0 && biogenic_waste_input_conversion > 0.0"),
          "bionaphtha_output_conversion * biogenic_waste_input_conversion / (1 - loss_output_conversion) * output_of_loss"
        )
      ), 
      BILLIONS
    )
- unit = PJ
