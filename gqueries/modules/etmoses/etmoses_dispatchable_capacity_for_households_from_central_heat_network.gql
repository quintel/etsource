# The amount of the installed capacity from the central heat network which is used for households
# and produced by must-run sources

- query = 
    Q(etmoses_installed_capacity_of_dispatchable_central_heat_network) * Q(etmoses_households_fraction_of_central_heat_network)
    
- unit = MW