- query =
    DIVIDE(
      SUM(
        V(households_space_heater_combined_network_gas, capital_expenditures_excluding_ccs_per(:mw_heat)),
        V(households_space_heater_combined_network_gas, operating_expenses_excluding_ccs_per(:mw_heat))
      ),
      1000
    )
- unit = euro
