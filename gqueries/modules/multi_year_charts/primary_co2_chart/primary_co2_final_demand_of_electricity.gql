# The primary co2 of final demand electricity

- unit = tonne
- query = V(INTERSECTION(G(final_demand_group),USE(energetic)).select {|node| node.input_slots.detect { |slot| slot.carrier.electricity? }}, primary_co2_emission).sum / THOUSANDS
