# CO2 of carrier group 'hydrogen' in agriculture

- unit = tonne
- query =
    SUM(
      V(INTERSECTION(INTERSECTION(G(final_demand_group),SECTOR(agriculture)),USE(energetic)).select {|node| node.input_slots.detect { |slot| slot.carrier.hydrogen? }}, "primary_co2_emission")
    ) / THOUSANDS
