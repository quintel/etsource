# CO2 of carrier group 'coal_and_derivatives' in households

- unit = tonne
- query =
    SUM(
      V(INTERSECTION(INTERSECTION(G(final_demand_group),SECTOR(households)),USE(energetic)).select {|node| node.input_slots.detect { |slot| slot.carrier.coal? }}, "primary_co2_emission"),
      V(INTERSECTION(INTERSECTION(G(final_demand_group),SECTOR(households)),USE(energetic)).select {|node| node.input_slots.detect { |slot| slot.carrier.coal_gas? }}, "primary_co2_emission")
    ) / THOUSANDS
