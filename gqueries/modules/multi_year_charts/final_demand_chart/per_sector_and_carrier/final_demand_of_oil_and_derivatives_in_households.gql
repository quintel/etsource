# Energetic final demand of the 'oil_and_derivatives' carrier group

- unit = MJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            FILTER(
              LG(final_demand),"sector == :households"
            ),
            energetic?
          ),
          crude_oil?
        ),
        value
      ),
      V(
        FILTER(
          FILTER(
            FILTER(
              LG(final_demand),"sector == :households"
            ),
            energetic?
          ),
          gasoline?
        ),
        value
      ),
      V(
        FILTER(
          FILTER(
            FILTER(
              LG(final_demand),"sector == :households"
            ),
            energetic?
          ),
          diesel?
        ),
        value
      ),
      V(
        FILTER(
          FILTER(
            FILTER(
              LG(final_demand),"sector == :households"
            ),
            energetic?
          ),
          lpg?
        ),
        value
      ),
      V(
        FILTER(
          FILTER(
            FILTER(
              LG(final_demand),"sector == :households"
            ),
            energetic?
          ),
          kerosene?
        ),
        value
      ),
      V(
        FILTER(
          FILTER(
            FILTER(
              LG(final_demand),"sector == :households"
            ),
            energetic?
          ),
          heavy_fuel_oil?
        ),
        value
      )
    )
