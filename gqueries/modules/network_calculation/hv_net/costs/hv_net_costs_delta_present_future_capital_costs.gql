# The capital costs of the future hv net

- query =
          SUM(
              (QUERY_FUTURE(hv_net_costs_delta_present_future_investment_costs) / 2.0) * Q(hv_net_costs_delta_present_future_wacc)
          )

- unit = euro
