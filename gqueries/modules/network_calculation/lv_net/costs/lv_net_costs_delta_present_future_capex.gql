# The CAPEX of the future lv net

- query =
          SUM(
              QUERY_FUTURE(lv_net_costs_delta_present_future_capital_costs),
              QUERY_FUTURE(lv_net_costs_delta_present_future_depreciation_costs)
          )

- unit = euro
