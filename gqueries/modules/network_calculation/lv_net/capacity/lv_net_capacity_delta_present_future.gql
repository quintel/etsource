# The delta (difference) between the low voltage net capacity in the present and the future.

- query = MAX(0, QUERY_FUTURE(lv_net_capacity_future) - QUERY_PRESENT(lv_net_capacity_present))

- unit = MW
