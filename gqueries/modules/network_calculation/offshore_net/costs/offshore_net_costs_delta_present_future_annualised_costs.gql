# The delta (difference) between the present and the future costs of the offshore net.

- query =
          SUM(
              QUERY_FUTURE(offshore_net_costs_delta_present_future_capex),
              QUERY_FUTURE(offshore_net_costs_delta_present_future_opex)
          )

- unit = euro
