# The total costs of the network calculation

- query =
          SUM(
              QUERY_PRESENT(offshore_net_costs_present_annualised_costs),
              QUERY_FUTURE(offshore_net_costs_delta_present_future_annualised_costs)
          )

- unit = euro
