# The delta (difference) between the present and the future costs of the medium voltage net.

- query =
          SUM(
              QUERY_FUTURE(mv_net_additional_CAPEX),
              QUERY_FUTURE(mv_net_additional_OPEX)
          )

- unit = euro
