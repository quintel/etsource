# The costs of the future mv net

- query =
          SUM(
              QUERY_FUTURE(mv_net_costs_delta_present_future_capital_costs),
              QUERY_FUTURE(mv_net_costs_delta_present_future_depreciation_costs)
          )

- unit = euro
