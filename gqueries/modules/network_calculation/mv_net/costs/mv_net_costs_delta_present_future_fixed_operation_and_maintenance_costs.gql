# The operations and maintenance costs of the future mv net, consisting of a fixed component only

- query = V(energy_power_mv_distribution_network_electricity,fixed_operation_and_maintenance_costs_per(:mw_input)) * QUERY_FUTURE(mv_net_capacity_delta_present_future)

- unit = euro
