# The OPEX of the future mv net, consisting of operation and maintenance costs

- query =
          SUM(
              QUERY_FUTURE(mv_net_costs_delta_present_future_fixed_operation_and_maintenance_costs)
          )

- unit = euro
