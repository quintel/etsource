# The delta (difference) between the present and the future costs of the medium voltage net.

- query =
          SUM(
              QUERY_FUTURE(mv_net_costs_delta_present_future_capex),
              QUERY_FUTURE(mv_net_costs_delta_present_future_opex)
          )

- unit = euro
