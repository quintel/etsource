# The depreciation costs of the future mv net

- query =
          SUM(
              QUERY_FUTURE(mv_net_costs_delta_present_future_investment_costs) / Q(mv_net_costs_delta_present_future_technical_lifetime)
          )

- unit = euro
