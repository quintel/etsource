# The delta (difference) between the low voltage net capacity in the present and the future.

- query = MAX(QUERY_FUTURE(mv_hv_trafo_capacity_future) - QUERY_PRESENT(mv_hv_trafo_capacity_present))

- unit = MW
