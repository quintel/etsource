# The CAPEX of the future lv-mv transformers

- query =
          SUM(
              QUERY_FUTURE(lv_mv_trafo_costs_delta_present_future_capital_costs),
              QUERY_FUTURE(lv_mv_trafo_costs_delta_present_future_depreciation_costs)
          )

- unit = euro
