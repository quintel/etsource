# The capital costs of the future lv-mv transformers

- query =
          SUM(
              QUERY_FUTURE(lv_mv_trafo_costs_delta_present_future_investment_costs) / 2.0 * Q(lv_mv_trafo_costs_delta_present_future_wacc)
          )

- unit = euro
