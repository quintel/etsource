# The depreciation costs of the future interconnection net

- query =
          SUM(
              QUERY_FUTURE(interconnection_net_investment_costs) / V(energy_power_hv_network_electricity,technical_lifetime)
          )

- unit = euro
