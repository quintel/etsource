# The capital costs of the future interconnection net

- query =
          SUM(
              QUERY_FUTURE(interconnection_net_costs_delta_present_future_investment_costs) / 2.0 * Q(interconnection_net_costs_delta_present_future_wacc)
          )

- unit = euro
