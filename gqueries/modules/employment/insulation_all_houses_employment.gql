# The total man-years spent in NL on insulation of new houses.

- unit = man_years
- query =
    AREA(households_insulation_employment_constant) * Q(costs_of_insulation_households) / AREA(man_hours_per_man_year)
