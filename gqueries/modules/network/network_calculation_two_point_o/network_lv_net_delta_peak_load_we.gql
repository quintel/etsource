#

- unit = mw

SUM(
    V(Q(temp_query_network_lv_net_converters),"
        simult_we*
        (electricity_output_conversion-electricity_input_conversion)*
        typical_input_capacity_in_mw*
        (number_of_units - peak_load_units_present) "
    )
)