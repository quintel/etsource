#

- unit = mw

SUM(
    PRODUCT(
        Q(network_mv_d_net_delta_peak_load_sd),
        V(mv_distribution_network_energy_energetic, simult_sd),
        V(mv_transport_network_energy, simult_sd)
    ),
    SUM(
        V(Q(temp_query_network_hv_mv_trafo_converters),"
            simult_sd*
            (electricity_output_conversion+electricity_input_conversion)*
            typical_input_capacity_in_mw*
            (number_of_units - peak_load_units_present)"
        )
    )
)