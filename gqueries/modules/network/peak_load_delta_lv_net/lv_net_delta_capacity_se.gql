- deprecated_key = pl_lv_net_delta_capacity_se
# In theory peakload delt heaters needs to be taken into account, but they have no demand during the summer
# 
# SUM(
#   Q(peak_load_delta_electric_cars_se),
#   Q(peak_load_delta_electric_trucks_se),
#   Q(peak_load_delta_household_coolers)
# )

- unit = mw

SUM(
  Q(peak_load_delta_electric_cars_se),
  Q(peak_load_delta_electric_trucks_se), 
  Q(peak_load_delta_household_coolers),
  Q(peak_load_delta_electric_boiler)

)