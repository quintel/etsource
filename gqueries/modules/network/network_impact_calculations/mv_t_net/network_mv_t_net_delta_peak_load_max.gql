# Max newly required capacity for the MV-transport network
# Compares the required capacity for 4 periods of the year

- unit = MW

MAX(
    Q(network_mv_t_net_delta_peak_load_we),
    Q(network_mv_t_net_delta_peak_load_wd),
    Q(network_mv_t_net_delta_peak_load_se),
    Q(network_mv_t_net_delta_peak_load_sd)
)
