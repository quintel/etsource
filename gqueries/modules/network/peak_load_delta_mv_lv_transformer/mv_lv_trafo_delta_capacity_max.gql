- deprecated_key = pl_mv_lv_trafo_delta_capacity_max
# No technologies connected to mv/lv transformer therefore delta capacity max is equal to impact on lv-net * simult

- unit = mw

PRODUCT(
  Q(lv_net_delta_capacity_max),
  V( energy_power_lv_network_electricity , simult_wd )
)
