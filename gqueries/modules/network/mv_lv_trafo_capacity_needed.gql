- deprecated_key = pl_mv_lv_trafo_capacity_needed
# 

- unit = mw

SUM(
  MAX(
    SUM(
      Q(mv_lv_trafo_delta_capacity_max), 
      V(lv_mv_transformer_energy; network_capacity_used_in_mw) 
    ),
    V(lv_mv_transformer_energy; network_capacity_available_in_mw)
  ),
  NEG(
    V(lv_mv_transformer_energy; network_capacity_available_in_mw)
  )
)