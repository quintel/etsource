# 

- unit = converters

PRODUCT(
 DIVIDE(
  SUM(
   V(Q(buildings_cooling_converters);input_of_electricity)
  ),
  SUM(
   V(Q(buildings_cooling_converters);demand)
  )
 ),
V(cooling_buildings_energetic;input_of_cooling)
)