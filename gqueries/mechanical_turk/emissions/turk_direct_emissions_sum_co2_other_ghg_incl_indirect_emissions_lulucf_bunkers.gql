# Sum of CO2 emissions and other GHG emissions queries

- unit = MT
- query =
    SUM(
      Q(direct_emissions_co2_incl_indirect_emissions_lulucf_bunkers),
      Q(direct_emissions_other_ghg_incl_indirect_emissions_lulucf_bunkers)
    )
