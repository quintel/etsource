# Sum of all energy input flows of the oily node in the biomass sankey

- unit = PJ
- query =
    SUM(
      Q(import_oily_biomass_in_biomass_sankey),
      Q(local_oily_biomass_in_biomass_sankey)
    )
