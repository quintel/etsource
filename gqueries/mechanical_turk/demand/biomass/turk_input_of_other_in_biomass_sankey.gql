# Sum of all energy input flows of the other node in the biomass sankey

- unit = PJ
- query =
    SUM(
      Q(dry_biomass_to_other_in_biomass_sankey),
      Q(greengas_to_other_in_biomass_sankey)
    )
