# 

- unit = mw

SUM(
  MAX(
    SUM(
      Q(pl_mv_d_net_delta_capacity_max), 
      V(mv_distribution_network_energy_energetic; network_capacity_used_in_mw) 
    ),
    V(mv_distribution_network_energy_energetic; network_capacity_available_in_mw)
  ),
  NEG(
    V(mv_distribution_network_energy_energetic; network_capacity_available_in_mw)
  )
)