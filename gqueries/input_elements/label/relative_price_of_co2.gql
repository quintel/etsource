# Relative price of CO2

- query =
    100.0 * DIVIDE(
      SUM(QUERY_FUTURE(-> {Q(price_of_co2)}), PRODUCT(-1,QUERY_PRESENT(-> {Q(price_of_co2)}))),
      QUERY_PRESENT(-> {Q(price_of_co2)}))

- unit = %
