- deprecated_key = max_production_in_mw_wind_offshore
# 

- unit = mw

V(
 PRODUCT(
  DIVIDE(AREA(onshore_suitable_for_wind),V(wind_offshore_energy,land_use_per_unit)),
  V(wind_offshore_energy,"typical_electricity_production_capacity * full_load_hours / 8760")
 )
)