# The average sustainability of electricity produced by oil plants, given in KG per MJ. This value can be used to determine the attribute of foreign_oil_electricity if exported or imported.

- unit = factor

DIVIDE(
   SUM(V(Q(electricity_producing_converters_oil);sustainability_share * demand)),
   SUM(V(Q(electricity_producing_converters_oil);demand))
)