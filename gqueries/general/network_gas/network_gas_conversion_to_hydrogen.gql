- query =
    DIVIDE(
      SUM(
        V(
          EXCLUDE(
            G(hydrogen_production),
            G(industry_transformation)
            ),
          "input_of_network_gas + input_of_natural_gas + input_of_greengas"
        )
      ),
      BILLIONS
    )
- unit = PJ
