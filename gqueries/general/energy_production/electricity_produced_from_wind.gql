# Returns the produced electricity using wind.
# TO DO:
# It should work directly by looking to converters directly fed by the "wind" carrier that have output of electricity.

- unit = mj

SUM(
  V(
    G(electricity_production);output_of_electricity * wind_input_conversion
  )
)
