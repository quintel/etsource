# Returns the produced electricity using wind.

# It works by looking to converters directly fed by the "wind" converter that have output of electricity.

- unit = MJ
- query = V(Q(electricity_producing_wind_converters),output_of_electricity).sum
