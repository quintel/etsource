- deprecated_key = buildings_cooling_electricity
# 

- unit = converters

PRODUCT(
 DIVIDE(
  SUM(
   V(Q(cooling_converters_in_buildings),input_of_electricity)
  ),
  SUM(
   V(Q(cooling_converters_in_buildings),demand)
  )
 ),
V(cooling_buildings_energetic,input_of_cooling)
)