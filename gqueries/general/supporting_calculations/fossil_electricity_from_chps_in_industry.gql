# The electricity from the gas CHP in industry, excluding the part from the sustainable green gas.

- unit = pj

DIVIDE(

SUM(V(small_chp_industry_energetic;output_of_electricity),

NEG(SUM(V(small_chp_industry_energetic;output_of_electricity * sustainability_share))))

,BILLIONS)
