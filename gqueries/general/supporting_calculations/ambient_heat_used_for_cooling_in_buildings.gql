- deprecated_key = buildings_cooling_ambient
# 

- unit = mj

PRODUCT(
 DIVIDE(
  SUM(
   V(Q(buildings_cooling_converters);input_of_ambient_cold)
  ),
  SUM(
   V(Q(buildings_cooling_converters);demand)
  )
 ),
V(cooling_buildings_energetic;input_of_cooling)
)