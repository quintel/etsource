# The share of energetic useable heat in aggregated other industry

- query =
    DIVIDE(
        Q(final_demand_of_heat_in_other_non_specified_industry_energetic),
        Q(final_demand_of_other_non_specified_industry_energetic)
    )
- unit = factor
