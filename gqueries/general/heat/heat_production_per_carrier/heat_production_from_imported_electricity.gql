# Use of 'imported_electricity' carrier group in 'heat_production'

- unit = PJ
- query =
    SUM(
      V(G(heat_production),"input_of_imported_electricity * heat_and_cold_output_conversion")
    ) / BILLIONS
