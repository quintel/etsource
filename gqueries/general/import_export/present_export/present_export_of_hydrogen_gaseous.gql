# Import of coal and derivatives

- query =
    DIVIDE(
      SUM(
        V(
          G(energy_export),
          input_of(
            hydrogen_gaseous
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
