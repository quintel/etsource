# Import of coal and derivatives

- query =
    present:DIVIDE(
      SUM(
        V(
          G(energy_export),
          output_of(
            ammonia
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
