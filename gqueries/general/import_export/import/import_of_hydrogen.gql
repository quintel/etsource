# Import of hydrogen (gaseous)

- query =
    DIVIDE(
      SUM(
        V(
          G(energy_import),
          output_of(
            imported_hydrogen
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
