# Import of bio_pyrolysis_oil

- query =
    DIVIDE(
      SUM(
        V(
          G(energy_import),
          output_of(
            bio_pyrolysis_oil
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
