# Import of coal and derivatives

- query =
    DIVIDE(
      SUM(
        V(
          G(energy_import),
          output_of(
            imported_liquid_hydrogen
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
