# Import of bionaphtha

- query =
    DIVIDE(
      SUM(
        V(
          G(energy_import),
          output_of(
            bionaphtha
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
