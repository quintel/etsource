# Import of oil and derivatives

- query =
    DIVIDE(
      SUM(
        V(
          G(energy_import),
          output_of(
            crude_oil
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
