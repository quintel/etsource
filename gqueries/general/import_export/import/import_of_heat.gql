# Import of coal and derivatives

- query =
    DIVIDE(
      SUM(
        V(
          G(energy_import),
          output_of(
            heat
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
