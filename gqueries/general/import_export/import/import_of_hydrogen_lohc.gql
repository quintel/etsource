# Import of coal and derivatives

- query =
    DIVIDE(
      SUM(
        V(
          G(energy_import),
          output_of(
            imported_lohc
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
