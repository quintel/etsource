# Export of hydrogen (gaseous)

- query =
    DIVIDE(
      SUM(
        V(
          G(energy_export),
          input_of(
            hydrogen
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
