# Import of coal and derivatives

- query =
    DIVIDE(
      SUM(
        V(
          G(energy_export),
          input_of(
            liquid_hydrogen
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
