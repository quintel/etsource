- deprecated_key = electricity_dependence
# 

- unit = factor

DIVIDE(
  V(energy_interconnector_imported_electricity,demand),
  SUM(Q(total_electricity_produced), V(energy_interconnector_imported_electricity,demand))
)
