# Import of coal and derivatives

- query =
    Dpresent:IVIDE(
      SUM(
        V(
          G(energy_import),
          output_of(
            coal,
            coal_gas,
            lignite,
            cokes
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
