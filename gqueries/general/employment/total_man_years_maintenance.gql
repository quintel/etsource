# The total man-year spent in NL on maintenance.

- unit = man_year

DIVIDE(
	PRODUCT(
		SUM(
			V(Q(employment_converters);hours_maint_nl/technical_lifetime)),
			V(Q(scenario_duration)
		)
	),
	MAN_HOURS_PER_MAN_YEAR
)
