# The total man-year spent in NL on production.

- unit = man_year

DIVIDE(
	PRODUCT(
		SUM(
			V(Q(employment_converters);hours_prod_nl/technical_lifetime)),
			V(Q(scenario_duration)
		)
	),
	MAN_HOURS_PER_MAN_YEAR
)
