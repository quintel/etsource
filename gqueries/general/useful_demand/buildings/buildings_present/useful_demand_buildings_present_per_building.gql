# The unit GJ/# means Gigajoule per residence equivalent (woningequivalenten) in this case.
- query =
    PRODUCT(
      DIVIDE(
        Q(useful_demand_buildings_present),
        Q(number_of_buildings_present)
      ),
      MILLIONS
    )
- unit = GJ/#
