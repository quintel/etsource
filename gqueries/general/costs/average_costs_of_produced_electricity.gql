- deprecated_key = avg_total_cost_for_electricity_production_per_mj
# Fixed. Before the costs per MWh were calculated.
# 
# Deleted this :
# 
# PRODUCT(
#   SECS_PER_HOUR,

- unit = euro

  DIVIDE(
    SUM(V(G(electricity_production);total_costs)),
    SUM(V(G(electricity_production);output_of_electricity))
  )
