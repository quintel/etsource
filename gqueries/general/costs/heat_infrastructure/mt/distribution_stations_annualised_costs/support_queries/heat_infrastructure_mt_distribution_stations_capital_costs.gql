# Assuming linear depreciation, in line with ETM cost calculation

- query =
    PRODUCT(
      DIVIDE(
        Q(heat_infrastructure_mt_distribution_stations_investment_costs),
        2.0),
      Q(heat_infrastructure_mt_distribution_stations_wacc)
    )
- unit = euro
