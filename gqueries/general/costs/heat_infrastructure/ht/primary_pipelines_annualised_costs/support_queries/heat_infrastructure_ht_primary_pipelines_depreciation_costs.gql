# Assuming linear depreciation, in line with ETM cost calculation

- query =
    DIVIDE(
      Q(heat_infrastructure_ht_primary_pipelines_investment_costs),
      Q(heat_infrastructure_ht_primary_pipelines_technical_lifetime)
      )
- unit = euro
