# Primary CO2 of carrier group 'waste' for the export converter group and curtailment converter

- unit = tonne
- query =
    Q(primary_co2_of_waste_in_export) + Q(primary_co2_of_waste_in_curtailment)
