# CO2 of carrier group 'biomass_products' in transport

- unit = tonne
- query =
    SUM(
      V(INTERSECTION(G(final_demand_group),SECTOR(transport)).select {|node| node.output_slots.detect { |slot| slot.carrier.biodiesel? }}, primary_co2_emission),
      V(INTERSECTION(G(final_demand_group),SECTOR(transport)).select {|node| node.output_slots.detect { |slot| slot.carrier.bio_ethanol? }}, primary_co2_emission),
      V(INTERSECTION(G(final_demand_group),SECTOR(transport)).select {|node| node.output_slots.detect { |slot| slot.carrier.wood_pellets? }}, primary_co2_emission)      
    ) / THOUSANDS
