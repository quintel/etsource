# How much Mt CO2 is emitted yearly by the transport sector
# Used in transportation education module

- query = V( INTERSECTION(G(final_demand_group), SECTOR(transport)), primary_co2_emission).sum / 1_000_000_000
- unit = mt