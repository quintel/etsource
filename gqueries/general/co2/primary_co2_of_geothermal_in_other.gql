# CO2 of carrier group 'geothermal' in other

- unit = tonne
- query =
    SUM(
      V(INTERSECTION(G(final_demand_group),SECTOR(other)).select {|node| node.output_slots.detect { |slot| slot.carrier.geothermal? }}, primary_co2_emission)      
    ) / THOUSANDS
