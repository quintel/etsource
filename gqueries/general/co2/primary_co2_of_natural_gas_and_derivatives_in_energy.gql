# CO2 of carrier group 'natural_gas_and_derivatives' in energy

- unit = tonne
- query =
    SUM(
      V(INTERSECTION(G(final_demand_group),SECTOR(energy)).select {|node| node.slots.detect { |slot| slot.carrier.network_gas? }}, primary_co2_emission)      
    ) / THOUSANDS
