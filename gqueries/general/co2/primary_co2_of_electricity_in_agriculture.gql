# CO2 of carrier group 'electricity' in agriculture

- unit = tonne
- query =
    SUM(
      V(INTERSECTION(G(final_demand_group),SECTOR(agriculture)).select {|node| node.slots.detect { |slot| slot.carrier.electricity? }}, primary_co2_emission)      
    ) / THOUSANDS
