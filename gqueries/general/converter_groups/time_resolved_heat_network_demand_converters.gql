# Nodes that constitute heat_network demand. Includes nodes which participate as consumers and flex in time-resolved heat_network.

- query =
    FILTER(
      ALL(),
      "heat_network && (heat_network.type == :consumer || heat_network.type == :flex)"
    )
- unit = converters
