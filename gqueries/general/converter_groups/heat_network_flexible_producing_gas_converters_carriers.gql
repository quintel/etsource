- query =
    FILTER(
      Q(time_resolved_heat_network_production_flexible_converters_carriers),
      "gas_power_fuelmix_input_conversion > 0 || network_gas_input_conversion > 0"
    )
- unit = nodes