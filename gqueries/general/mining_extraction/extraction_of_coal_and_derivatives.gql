# Extraction of coal and derivatives

- query =
    DIVIDE(
      SUM(
        V(
          G(mining_and_extraction),
          output_of(
            coal,
            lignite
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
