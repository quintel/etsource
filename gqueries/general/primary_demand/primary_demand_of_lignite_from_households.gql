- unit = PJ
- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(households),G(final_demand_group)),primary_demand_of_lignite).sum,
      BILLIONS
    )
- deprecated_key = households_lignite_primary_demand
