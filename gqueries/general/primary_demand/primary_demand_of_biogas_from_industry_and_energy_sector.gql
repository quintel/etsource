- unit = PJ
- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(industry),G(final_demand_group)),INTERSECTION(SECTOR(energy),G(final_demand_group)),primary_demand_of_biogas).sum,
      BILLIONS
    )
- deprecated_key = industry_biogas_primary_demand
