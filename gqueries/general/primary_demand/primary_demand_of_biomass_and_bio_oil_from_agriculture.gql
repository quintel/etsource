# 

- deprecated_key = primary_demand_of_biomass_from_agriculture
- unit = pj

DIVIDE(
  SUM(
    V(
      INTERSECTION(SECTOR(agriculture),G(final_demand_cbs));
      primary_demand_of_biomass + primary_demand_of_corn + primary_demand_of_manure + primary_demand_of_bio_oil)
    ),
  BILLIONS
)