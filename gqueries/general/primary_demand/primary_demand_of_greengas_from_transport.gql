- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(transport),G(final_demand_group)),primary_demand_of_greengas).sum,
      BILLIONS
    )
- unit = pj
- deprecated_key = transport_greengas_primary_demand
