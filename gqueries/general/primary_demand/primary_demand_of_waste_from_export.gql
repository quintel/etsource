# Primary demand of carrier group 'waste' for the export converter group

- unit = PJ
- query =
    SUM(
       V(G(energy_export), primary_demand_of_non_biogenic_waste),
       V(G(energy_export), primary_demand_of_biogenic_waste)
    ) / BILLIONS
