- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(transport),G(final_demand_group)),primary_demand_of_lignite).sum,
      BILLIONS
    )
- unit = pj
- deprecated_key = transport_lignite_primary_demand
