- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          INTERSECTION(SECTOR(industry),G(final_demand_group)),
          primary_demand_of_uranium_oxide
        )
      ),
      BILLIONS
    )
- deprecated_key = industry_uranium_primary_demand
