- unit = PJ
- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(households),G(final_demand_group)),primary_demand_of_biogas).sum,
      BILLIONS
    )
- deprecated_key = households_biogas_primary_demand
