- unit = PJ
- query =
    DIVIDE(
      V(
        INTERSECTION(SECTOR(agriculture),G(final_demand_group)),
        primary_demand_of_electricity
      ).sum,
      BILLIONS
    )
- deprecated_key = agriculture_non_e_primary_demand
