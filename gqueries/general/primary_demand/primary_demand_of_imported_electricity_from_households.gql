- unit = PJ
- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(households),G(final_demand_group)),primary_demand_of_imported_electricity).sum,
      BILLIONS
    )
- deprecated_key = households_imported_e_primary_demand
