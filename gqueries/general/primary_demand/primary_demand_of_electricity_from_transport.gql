- unit = PJ
- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(transport),G(final_demand_group)),primary_demand_of_electricity).sum,
      BILLIONS
    )
- deprecated_key = transport_non_e_primary_demand
