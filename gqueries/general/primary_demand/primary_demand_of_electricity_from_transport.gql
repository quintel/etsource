- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(transport),G(final_demand_cbs)),primary_demand_of_electricity).sum,
      BILLIONS
    )
- unit = pj
- deprecated_key = transport_non_e_primary_demand
