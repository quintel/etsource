- unit = PJ
- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(households),
      G(final_demand_group)),"primary_demand_of_solar_thermal + primary_demand_of_steam_hot_water").sum,
      BILLIONS
    )
