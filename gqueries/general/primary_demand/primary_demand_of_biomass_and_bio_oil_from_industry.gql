# 

- deprecated_key = industry_biomass_primary_demand
- unit = mj

DIVIDE(
  SUM(
    V(
      INTERSECTION(SECTOR(industry,energy),G(final_demand_cbs));
      primary_demand_of_biomass + primary_demand_of_corn + primary_demand_of_manure + primary_demand_of_bio_oil)
    ),
  BILLIONS
)