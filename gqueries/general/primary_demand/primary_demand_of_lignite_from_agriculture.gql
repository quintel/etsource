- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(agriculture),G(final_demand_cbs)),primary_demand_of_lignite).sum,
      BILLIONS
    )
- unit = pj
- deprecated_key = agriculture_lignite_primary_demand
