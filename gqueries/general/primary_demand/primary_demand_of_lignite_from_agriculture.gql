- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(agriculture),G(final_demand_group)),primary_demand_of_lignite).sum,
      BILLIONS
    )
- unit = pj
- deprecated_key = agriculture_lignite_primary_demand
