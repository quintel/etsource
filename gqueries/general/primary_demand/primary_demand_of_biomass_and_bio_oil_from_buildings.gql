# 

- deprecated_key = buildings_biomass_primary_demand
- unit = pj

DIVIDE(
  SUM(
    V(
      INTERSECTION(SECTOR(buildings),G(final_demand_cbs));
      primary_demand_of_biomass + primary_demand_of_corn + primary_demand_of_manure + primary_demand_of_bio_oil)
    ),
  BILLIONS
)