- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          INTERSECTION(SECTOR(other),G(final_demand_group)),
          "primary_demand_of_wood_pellets + primary_demand_of_torrefied_biomass_pellets + primary_demand_of_dry_biomass")
        ),
      BILLIONS
    )
- deprecated_key = other_biomass_primary_demand
