- unit = PJ
- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(industry),G(final_demand_group)),INTERSECTION(SECTOR(energy),G(final_demand_group)),primary_demand_of_uranium_oxide).sum,
      BILLIONS
    )
- deprecated_key = industry_uranium_primary_demand
