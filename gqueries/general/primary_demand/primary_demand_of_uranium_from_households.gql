- unit = PJ
- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(households),G(final_demand_group)),primary_demand_of_uranium_oxide).sum,
      BILLIONS
    )
- deprecated_key = households_uranium_primary_demand
