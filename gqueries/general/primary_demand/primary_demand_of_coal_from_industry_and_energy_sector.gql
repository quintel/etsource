- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(industry),G(final_demand_group)),INTERSECTION(SECTOR(energy),G(final_demand_group)),primary_demand_of_coal).sum,
      BILLIONS
    )
- unit = pj
- deprecated_key = industry_coal_primary_demand
