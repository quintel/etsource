- query =
    DIVIDE(
      V(INTERSECTION(SECTOR(households),G(final_demand_cbs)),primary_demand_of_greengas).sum,
      BILLIONS
    )
- unit = pj
- deprecated_key = households_greengas_primary_demand
