- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(INTERSECTION(SECTOR(industry),G(final_demand_group)),primary_demand_of_imported_liquid_hydrogen),
        V(INTERSECTION(SECTOR(energy),G(final_demand_group)),primary_demand_of_imported_liquid_hydrogen)
      ),
      BILLIONS
    )
