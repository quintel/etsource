# Methanol to Transport

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            EG(final_demand),"sector == :bunkers"
          ),
          methanol?
        ),
        value
      )
    ) / BILLIONS
