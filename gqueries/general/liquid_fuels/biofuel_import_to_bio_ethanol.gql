# Import of bio_ethanol

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          G(energy_import),
          output_of(
            bio_ethanol
          )
        )
      ),
      BILLIONS
    )