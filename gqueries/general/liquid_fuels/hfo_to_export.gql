# Hfo to Export

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          G(energy_export),
          input_of(
            heavy_fuel_oil
          )
        )
      ),
      BILLIONS
    )
