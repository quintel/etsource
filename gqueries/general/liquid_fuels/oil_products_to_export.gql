# Oil Products to Export

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          G(energy_export),
          input_of(
            crude_oil
          )
        )
      ),
      BILLIONS
    )
