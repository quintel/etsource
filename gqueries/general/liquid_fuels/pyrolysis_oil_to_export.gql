# Export of pyrolysis oil (baseload and backup)

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          G(energy_export),
          input_of(
            pyrolysis_oil
          )
        )
      ),
      BILLIONS
    )