# Hfo to Transport

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            EG(final_demand),"sector == :transport"
          ),
          heavy_fuel_oil?
        ),
        value
      )
    ) / BILLIONS
