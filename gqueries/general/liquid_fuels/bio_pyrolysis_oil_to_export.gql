# Export of bio pyrolysis oil (baseload and backup)

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          G(energy_export),
          input_of(
            bio_pyrolysis_oil
          )
        )
      ),
      BILLIONS
    )