# Kerosene to Transport

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            EG(final_demand),"sector == :transport"
          ),
          kerosene?
        ),
        value
      )
    ) / BILLIONS
