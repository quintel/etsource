# Direct conversion input of hydrogen for central heat production

- query =
    DIVIDE(
      SUM(
        V(
          Q(central_heat_producing_converters_sankey),
          input_of(
            hydrogen
          )
        )
      ),
      BILLIONS
    )
- unit = PJ
