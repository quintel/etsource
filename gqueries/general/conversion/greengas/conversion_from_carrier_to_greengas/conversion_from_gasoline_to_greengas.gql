# Direct conversion input of gasoline for greengas
# The function is broken down due to limited allowed inputs in V() function

- query =
    DIVIDE(
      V(
        G(biomass_products_production),
        "greengas_output_conversion / (1 - loss_output_conversion) * input_of_gasoline"
      ),
      BILLIONS
    )
- unit = PJ
