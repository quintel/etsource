# Direct conversion input of electricityimported_electricity for central heat production

- query = 
    DIVIDE(
      SUM(
        V(
          G(network_gas_production),
          "natural_gas_output_conversion + greengas_output_conversion / (1 - loss_output_conversion) * input_of_electricityimported_electricity"
        )
      ), 
      BILLIONS
    )
- unit = PJ
