# The final demand in the transport sector.

- unit = MJ
- query =
  SUM(
    V(
      FILTER(
          FILTER(
            LG(final_demand),"sector == :transport"
          ),
          energetic?
        ),
      value
    )
  )
