# The final demand in the bunkers sector.

- unit = MJ
- query =
  SUM(
    V(
      FILTER(
          FILTER(
            LG(final_demand),"sector == :bunkers"
          ),
          energetic?
        ),
      value
    )
  )
