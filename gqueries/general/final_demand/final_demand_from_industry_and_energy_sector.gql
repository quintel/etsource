# The final demand in the industry and energy sector.

- unit = mj

SUM(
  V(INTERSECTION(G(final_demand_cbs),SECTOR(industry)),demand),
  V(INTERSECTION(G(final_demand_cbs),SECTOR(energy)),demand)
)
