# Final demand of the 'bionaphtha' carrier group

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          FILTER(
            FILTER(
              EG(final_demand),"sector == :industry"
            ),
            bionaphtha?
          ),
          value
        )
      ),
      BILLIONS
    )
