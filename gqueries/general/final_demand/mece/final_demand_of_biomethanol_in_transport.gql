# Final demand of the 'biomethanol' carrier group

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          FILTER(
            FILTER(
              EG(final_demand),"sector == :transport"
            ),
            biomethanol?
          ),
          value
        )
      ),
      BILLIONS
    )
