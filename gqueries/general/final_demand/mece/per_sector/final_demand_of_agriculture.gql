# Final demand of the 'agriculture' sector

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          FILTER(
            EG(final_demand),"sector == :agriculture"
          ),
          value
        )
      ),
    BILLIONS
    )
