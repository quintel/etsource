# Final demand of the 'bio_kerosene' carrier group for households
# Input of bio_kerosene in crude_oil final demand is accounted for

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          FILTER(
            FILTER(
              EG(final_demand),"sector == :households"
            ),
            bio_kerosene?
          ),
          value
        ),
        PRODUCT(
          SUM(
            V(
              FILTER(
                FILTER(
                  FILTER(
                    EG(final_demand),"sector == :households"
                  ),
                  energetic?
                ),
                crude_oil?
              ),
              value
            )
          ),
          V(households_final_demand_crude_oil, bio_kerosene_input_conversion)
        )
      ),
      BILLIONS
    )
