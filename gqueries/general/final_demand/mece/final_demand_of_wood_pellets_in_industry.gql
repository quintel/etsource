# Final demand of the 'wood_pellets' carrier group

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          FILTER(
            FILTER(
              EG(final_demand),"sector == :industry"
            ),
            wood_pellets?
          ),
          value
        )
      ),
      BILLIONS
    )
