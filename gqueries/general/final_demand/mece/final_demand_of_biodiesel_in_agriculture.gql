# Final demand of the 'biodiesel' carrier group for agriculture
# Input of biodiesel in crude_oil final demand is accounted for

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          FILTER(
            FILTER(
              EG(final_demand),"sector == :agriculture"
            ),
            biodiesel?
          ),
          value
        ),
        PRODUCT(
          SUM(
            V(
              FILTER(
                FILTER(
                  FILTER(
                    EG(final_demand),"sector == :agriculture"
                  ),
                  energetic?
                ),
                crude_oil?
              ),
              value
            )
          ),
          V(agriculture_final_demand_crude_oil, biodiesel_input_conversion)
        )
      ),
      BILLIONS
    )
