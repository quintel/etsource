# Final demand of the 'bio_lng' carrier group

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          FILTER(
            FILTER(
              EG(final_demand),"sector == :transport"
            ),
            bio_lng?
          ),
          value
        )
      ),
      BILLIONS
    )
