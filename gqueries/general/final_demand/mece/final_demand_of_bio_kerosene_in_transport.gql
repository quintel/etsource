# Final demand of the 'bio_kerosene' carrier group

- unit = PJ
- query =
    DIVIDE(
      SUM(
        V(
          FILTER(
            FILTER(
              EG(final_demand),"sector == :transport"
            ),
            bio_kerosene?
          ),
          value
        )
      ),
      BILLIONS
    )
