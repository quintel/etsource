# Final demand of the 'environment' carrier group

- unit = pj
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            LG(final_demand),LG(chemical)
          ),
          ambient_heat?
        ),
        value
      ),
      V(
        FILTER(
          FILTER(
            LG(final_demand),LG(chemical)
          ),
          ambient_cold?
        ),
        value
      ),
      V(
        FILTER(
          FILTER(
            LG(final_demand),LG(chemical)
          ),
          solar_radiation?
        ),
        value
      ),
      V(
        FILTER(
          FILTER(
            LG(final_demand),LG(chemical)
          ),
          solar_thermal?
        ),
        value
      ),
      V(
        FILTER(
          FILTER(
            LG(final_demand),LG(chemical)
          ),
          geothermal?
        ),
        value
      )
    ) / BILLIONS
