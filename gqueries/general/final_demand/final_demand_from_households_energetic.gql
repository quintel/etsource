# The final demand in the households sector.

- unit = MJ
- query =
  SUM(
    V(
      FILTER(
          FILTER(
            LG(final_demand),"sector == :households"
          ),
          energetic?
        ),
      value
    )
  )
