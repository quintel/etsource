- query =
    DIVIDE(SUM(
    V(INTERSECTION(SECTOR(energy),G(final_demand_cbs)),input_of_biodiesel),
    V(INTERSECTION(SECTOR(energy),G(final_demand_cbs)),input_of_bio_ethanol)),BILLIONS)
- unit = pj
- deprecated_key = all_biofuels_energy_final
