- query =
    DIVIDE(
      SUM(
        V(INTERSECTION(SECTOR(industry),G(final_demand_cbs)),input_of_network_gas),V(INTERSECTION(SECTOR(energy),G(final_demand_cbs)),input_of_network_gas),
        V(INTERSECTION(SECTOR(industry),G(final_demand_cbs)),input_of_greengas),V(INTERSECTION(SECTOR(energy),G(final_demand_cbs)),input_of_greengas)
      ),
      BILLIONS
    )
- unit = pj
- deprecated_key = industry_final_demand_gas_and_green_gas
