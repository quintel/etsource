# Energetic final demand of the 'geothermal' carrier group in households cooling

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            INTERSECTION(EG(final_demand),EG(cooling_households)), "geothermal?"
          ),
          "energetic?"
        ),
        value
      )
    ) / BILLIONS
