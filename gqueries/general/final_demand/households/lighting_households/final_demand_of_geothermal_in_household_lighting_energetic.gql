# Energetic final demand of the 'geothermal' carrier group in households lighting

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            INTERSECTION(EG(final_demand),EG(lighting_households)), "geothermal?"
          ),
          "energetic?"
        ),
        value
      )
    ) / BILLIONS
