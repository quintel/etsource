# The final demand in the agriculture sector.

- unit = MJ
- query =
  SUM(
    V(
      FILTER(
          FILTER(
            LG(final_demand),"sector == :agriculture"
          ),
          energetic?
        ),
      value
    )
  )
