# Energetic final demand of the 'hydrogen' carrier group

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            INTERSECTION(
              LG(final_demand),LG(other_non_specified_industry)
            ),
            energetic?
          ),
          hydrogen?
        ),
        value
      )
    ) / BILLIONS
