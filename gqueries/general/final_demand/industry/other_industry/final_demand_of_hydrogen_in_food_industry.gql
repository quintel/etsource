# Final demand of the 'hydrogen' carrier group

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          INTERSECTION(
            LG(final_demand),LG(food_industry)
          ),
          hydrogen?
        ),
        value
      )
    ) / BILLIONS
