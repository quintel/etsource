# Energetic final demand of the 'hydrogen' carrier group

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            INTERSECTION(
              LG(final_demand),LG(aggregated_other_industry_nl)
            ),
            energetic?
          ),
          hydrogen?
        ),
        value
      )
    ) / BILLIONS
