# Final demand of the 'ambient' carrier group

- unit = pj
- query =
    SUM(
      V(
        FILTER(
          INTERSECTION(
            LG(final_demand),LG(other_industry)
          ),
          ambient_heat?
        ),
        value
      ),
      V(
        FILTER(
          INTERSECTION(
            LG(final_demand),LG(other_industry)
          ),
          ambient_cold?
        ),
        value
      )
    ) / BILLIONS
