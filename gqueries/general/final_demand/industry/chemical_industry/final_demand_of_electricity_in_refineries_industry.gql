# Final demand of the 'electricity' carrier group

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          INTERSECTION(
            LG(final_demand),LG(refineries_industry)
          ),
          electricity?
        ),
        value
      )
    ) / BILLIONS
