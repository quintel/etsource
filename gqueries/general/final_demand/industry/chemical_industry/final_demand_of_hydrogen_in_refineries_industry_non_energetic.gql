# Non-energetic final demand of the 'hydrogen' carrier group

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            INTERSECTION(
              LG(final_demand),LG(refineries_industry)
            ),
            "! energetic?"
          ),
          hydrogen?
        ),
        value
      )
    ) / BILLIONS
