# Final demand of the 'hydrogen' carrier group

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          INTERSECTION(
            LG(final_demand),LG(refineries_industry)
          ),
          hydrogen?
        ),
        value
      )
    ) / BILLIONS
