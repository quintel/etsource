# Energetic demand of bio-ethanol for freight shipping transport

- query = 
    SUM(
      V(
        FILTER(
          FILTER(
            INTERSECTION(
              EG(final_demand),EG(freight_transport),EG(shipping_transport)
            ),
            energetic?
          ),
          bio_ethanol?
        ),
        value
      )
    )
- unit = MJ
