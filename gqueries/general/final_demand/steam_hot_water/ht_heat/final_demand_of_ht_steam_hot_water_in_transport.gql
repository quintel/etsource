# Final demand of steam hot water of HT temperature

- unit = PJ
- query =
    DIVIDE(
      V(
        FILTER(
          FILTER(
            INTERSECTION(EG(final_demand),EG(ht_heat)),"sector == :transport"
          ),
          steam_hot_water?
        ),
        value
      ),
      BILLIONS
    )
