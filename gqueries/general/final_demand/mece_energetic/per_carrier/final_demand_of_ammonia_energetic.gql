# Energetic final demand of the 'ammonia' carrier group

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            EG(final_demand), "ammonia?"
          ),
          energetic?
        ),
        value
      )
    ) / BILLIONS
