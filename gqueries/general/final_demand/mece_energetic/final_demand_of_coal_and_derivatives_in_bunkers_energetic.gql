# Final demand of the 'coal_and_derivatives' carrier group

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            FILTER(
              EG(final_demand),"sector == :bunkers"
            ),
            energetic?
          ),
          coal?
        ),
        value
      ),
      V(
        FILTER(
          FILTER(
            FILTER(
              EG(final_demand),"sector == :bunkers"
            ),
            energetic?
          ),
          coal_gas?
        ),
        value
      ),
      V(
        FILTER(
          FILTER(
            FILTER(
              EG(final_demand),"sector == :bunkers"
            ),
            energetic?
          ),
          cokes?
        ),
        value
      )
    ) / BILLIONS
