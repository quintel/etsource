# Final demand of the 'electricity' carrier group

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            FILTER(
              LG(final_demand),"sector == :energy"
            ),
            energetic?
          ),
          electricity?
        ),
        value
      )      
    ) / BILLIONS
