# Final demand of the 'electricity' carrier group

- unit = PJ
- query =
    SUM(
      V(
        FILTER(
          FILTER(
            FILTER(
              LG(final_demand),"sector == :other"
            ),
            energetic?
          ),
          electricity?
        ),
        value
      )      
    ) / BILLIONS
