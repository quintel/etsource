# Returns the share of gasoline mix cars in car technology split

- query =
    DIVIDE(
      V(transport_car_using_gasoline_mix,output_of_passenger_kms),
      V(transport_useful_demand_cars,demand)
    )
- unit = %
