# Returns the share of compressed natural gas trucks in truck technology split

- query =
    DIVIDE(
      V(transport_truck_using_compressed_natural_gas,output_of_freight_tonne_kms),
      V(transport_useful_demand_trucks,demand)
    )
- unit = %
