# Returns the share of diesel mix cars in car technology split

- query =
    DIVIDE(
      V(transport_car_using_diesel_mix,output_of_passenger_kms),
      V(transport_useful_demand_cars,demand)
    )
- unit = %
