# Use of oily_biomass in transport sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(transport)), demand_of_bio_resources_including_abroad_of(oily_biomass)
    ).sum / BILLIONS
