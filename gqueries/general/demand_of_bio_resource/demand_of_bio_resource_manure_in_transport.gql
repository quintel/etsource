# Use of manure in transport sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(transport)), demand_of_bio_resources_including_abroad_of(manure)
    ).sum / BILLIONS
