# Use of sugar_cane in buildings sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(buildings)), demand_of_bio_resources_including_abroad_of(sugar_cane)
    ).sum / BILLIONS
