# Use of woody_biomass in transport sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(transport)), demand_of_bio_resources_including_abroad_of(woody_biomass)
    ).sum / BILLIONS
