# Use of sugar_beets in transport sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(transport)), demand_of_bio_resources_including_abroad_of(sugar_beets)
    ).sum / BILLIONS
