# Use of biogenic_waste in bunkers sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(bunkers)), demand_of_bio_resources_including_abroad_of(biogenic_waste)
    ).sum / BILLIONS
