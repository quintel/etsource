# Use of wood in households sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(households)), demand_of_bio_resources_including_abroad_of(wood)
    ).sum / BILLIONS
