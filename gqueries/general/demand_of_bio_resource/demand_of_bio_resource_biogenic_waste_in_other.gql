# Use of biogenic_waste in other sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(other)), demand_of_bio_resources_including_abroad_of(biogenic_waste)
    ).sum / BILLIONS
