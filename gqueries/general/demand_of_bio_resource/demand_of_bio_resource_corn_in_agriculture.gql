# Use of corn in agriculture sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(agriculture)), demand_of_bio_resources_including_abroad_of(corn)
    ).sum / BILLIONS
