# Use of sugar_beets in households sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(households)), demand_of_bio_resources_including_abroad_of(sugar_beets)
    ).sum / BILLIONS
