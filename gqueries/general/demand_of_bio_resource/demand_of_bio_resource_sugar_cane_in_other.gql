# Use of sugar_cane in other sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(other)), demand_of_bio_resources_including_abroad_of(sugar_cane)
    ).sum / BILLIONS
