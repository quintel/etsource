# Use of waste_fats in energy sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(energy)), demand_of_bio_resources_including_abroad_of(waste_fats)
    ).sum / BILLIONS
