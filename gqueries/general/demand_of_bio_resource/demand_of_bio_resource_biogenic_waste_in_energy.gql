# Use of biogenic_waste in energy sector

- unit = PJ
- query =
    V(
        INTERSECTION(G(final_demand_group),SECTOR(energy)), demand_of_bio_resources_including_abroad_of(biogenic_waste)
    ).sum / BILLIONS
