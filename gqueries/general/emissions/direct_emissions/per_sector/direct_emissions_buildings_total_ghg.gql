- unit = MT
- query =
    DIVIDE(
      SUM(
        V(
          G(emissions_buildings),
          direct_reporting_emissions_total_ghg_emissions
        ),
        MV(
          MG(emissions_buildings),
          direct_reporting_emissions_total_ghg_emissions
        )
      ),
      BILLIONS
    )
