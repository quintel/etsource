- unit = MT
- query =
    DIVIDE(
      SUM(
        V(
          SECTOR(ipcc_crt_code_agg, "1.A.4.b"),
          direct_reporting_emissions_total_ghg_emissions
        ),
        MV(
          MSECTOR(ipcc_crt_code_agg, "1.A.4.b"),
          direct_reporting_emissions_total_ghg_emissions
        )
      ),
      BILLIONS
    )