- unit = MT
- query =
    DIVIDE(
      SUM(
        V(
          SECTOR(ipcc_crt_code_agg, "1.C"),
          direct_reporting_emissions_total_ghg_emissions
        ),
        MV(
          MSECTOR(ipcc_crt_code_agg, "1.C"),
          direct_reporting_emissions_total_ghg_emissions
        )
      ),
      BILLIONS
    )