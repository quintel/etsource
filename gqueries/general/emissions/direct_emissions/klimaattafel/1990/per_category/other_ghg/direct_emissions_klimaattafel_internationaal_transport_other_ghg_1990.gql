# As the Klimaattafel mapping does not distinguish between international aviation and navigation
# and the emissions of aviation and navigation are dependent on seperate ETM inputs,
# the ETM sector bunkers query is used instead of a dedicated Klimaattafel query.

- unit = MT
- query = Q(direct_emissions_bunkers_other_ghg_1990)
