- unit = MT
- query =
    DIVIDE(
      SUM(
        V(
          SECTOR(klimaatsector, "Elektriciteit"),
          direct_reporting_emissions_total_ghg_emissions
        ),
        MV(
          MSECTOR(klimaatsector, "Elektriciteit"),
          direct_reporting_emissions_total_ghg_emissions
        )
      ),
      BILLIONS
    )
