- unit = MT
- query =
    DIVIDE(
      SUM(
        V(
          SECTOR(klimaatsector, "Internationaal transport"),
          direct_reporting_emissions_total_ghg_emissions
        ),
        MV(
          MSECTOR(klimaatsector, "Internationaal transport"),
          direct_reporting_emissions_total_ghg_emissions
        )
      ),
      BILLIONS
    )
