- unit = MT
- query =
    DIVIDE(
      SUM(
        V(
          SECTOR(klimaatsector, "Indirecte emissies"),
          direct_reporting_emissions_total_ghg_emissions
        ),
        MV(
          MSECTOR(klimaatsector, "Indirecte emissies"),
          direct_reporting_emissions_total_ghg_emissions
        )
      ),
      BILLIONS
    )
