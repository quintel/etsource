- unit = MT
- query =
    DIVIDE(
      SUM(
        V(
          SECTOR(klimaatsector, "Landbouw"),
          direct_reporting_emissions_total_ghg_emissions
        ),
        MV(
          MSECTOR(klimaatsector, "Landbouw"),
          direct_reporting_emissions_total_ghg_emissions
        )
      ),
      BILLIONS
    )
