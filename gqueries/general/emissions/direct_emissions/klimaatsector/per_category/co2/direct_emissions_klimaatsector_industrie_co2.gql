- unit = MT
- query =
    DIVIDE(
      SUM(
        V(
          SECTOR(klimaatsector, "Industrie"),
          "direct_reporting_emissions_co2_production - direct_reporting_emissions_co2_capture"
        ),
        MV(
          MSECTOR(klimaatsector, "Industrie"),
          "direct_reporting_emissions_co2_production - direct_reporting_emissions_co2_capture"
        )
      ),
      BILLIONS
    )
