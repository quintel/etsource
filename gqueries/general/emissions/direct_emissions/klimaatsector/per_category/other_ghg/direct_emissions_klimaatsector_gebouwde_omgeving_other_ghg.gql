- unit = MT
- query =
    DIVIDE(
      SUM(
        V(
          SECTOR(klimaatsector, "Gebouwde omgeving"),
          direct_reporting_emissions_other_ghg_emissions
        ),
        MV(
          MSECTOR(klimaatsector, "Gebouwde omgeving"),
          direct_reporting_emissions_other_ghg_emissions
        )
      ),
      BILLIONS
    )
