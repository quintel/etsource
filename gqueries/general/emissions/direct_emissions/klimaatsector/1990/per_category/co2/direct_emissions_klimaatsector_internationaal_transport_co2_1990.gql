# As the Klimaatsector mapping does not distinguish between international aviation and navigation
# and the emissions of aviation and navigation are dependent on seperate ETM inputs,
# the existing bunkers query is used instead of a dedicated Klimaatsector query.

- unit = MT
- query = Q(direct_emissions_bunkers_co2_1990)
