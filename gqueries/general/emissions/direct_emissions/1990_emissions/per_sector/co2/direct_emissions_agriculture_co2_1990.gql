- unit = mt
- query =
    DIVIDE(
      SUM(
        EMISSIONS(agriculture_non_specified, energetic, co2, 1990),
        EMISSIONS(agriculture_non_specified, non_energetic, co2, 1990)
      ),
      THOUSANDS
    )
