- unit = MT
- query =
    DIVIDE(
      SUM(
        V(
          SECTOR(emissions_sector, "International transport"),
          direct_reporting_emissions_other_ghg_emissions
        ),
        MV(
          MSECTOR(emissions_sector, "International transport"),
          direct_reporting_emissions_other_ghg_emissions
        )
      ),
      BILLIONS
    )
